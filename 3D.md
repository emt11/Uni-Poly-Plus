# GLT 3D 增量诊断与优化执行计划

计划 ID：`GLT-3D-GAIN-20260921-01` ｜ 修订：**r6** ｜ 日期：2026-09-21 UTC

状态：**D2 开发比较完成，待 Codex 审查**（r1 的 D0/D1、r2 的解释返修、r3 的 D2 实现与 smoke、
r4 的随机流与记录返修、r5 的 split 契约收口均已完成并保留；本文件 §9 含 r1–r6 记录）。
24 个 development units 全部 PASS（0 失败），**两个候选 FNORM／FSTABLE 均未达到预定门槛**，
聚合结论见 §9 r6 记录。

- **r6 授权范围**（用户明确交办）：执行记录更正与必要的开发调度／聚合代码；
  **F2D/FBASE/FNORM/FSTABLE × XC/EPS/EAT × fold0/1 共 24 个 development units，
  每单元最多 30 epochs、合计最多 720 epochs，patience=10**。
  **不授权** 预训练、D3、PH、新 probe、额外 seed、outer-test、OOF 或 refit；不追加独立 smoke。
  交付根目录 `results/glt_3d_gain_20260921/d2_development/`。
  完成后状态为“**D2 开发比较完成，待 Codex 审查**”，或准确报告阻断／部分完成。
  **本轮新授权不改变 r4 的历史超额记录**（GPU 前/反向实耗 16、原授权 8、超额 8，仍标记为未获追认）。
**r3 的 smoke 不构成公平性验收，也不能作为修复后的公平对照结果**：
其四臂并未共享同一条 O8/head 随机流（r4 修复），split 契约亦未收紧（r5 修复）。
**r4 修复的技术自检通过不等于用户追认其超预算消耗**（GPU 调用实耗 16、原授权 8、超额 8，见 §9 r4/r5 记录）。

- **r5 授权范围**（用户明确交办）：split 校验返修、CPU 合成测试与文档更新。
  **GPU、模型前向、backward、optimizer updates、微调 epochs 全部为 0**；
  不启动 24 units、D3、PH 或新 probe；保留全部历史产物；不重建、重写或替换现有 manifest。

- **r4 授权范围**（用户明确交办）：相关代码、文档与必要有界验证。
  **不启动** 24 development units、预训练、PH、D3 或新 probe；**不重跑** r3 的四个完整 epoch
  （r3 的 4 epochs / 36 updates 保留在 `d2_smoke/`）。状态先记为“D2 需返修”，返修后为“D2 r4 返修完成，待 Codex 审查”。

- **r3 授权范围**（用户明确交办）：文档与局部检查返修；D2 四臂实现及必要局部测试；
  四臂 `XC/fold0` 各至多 **1 epoch**（合计至多 4）。**不授权** 24 units、D3、PH、预训练、新 probe、额外 seed。
  D2 的候选判定仍须 24 units 的 macro3 与 XC 两折证据，本轮不产生晋级结论。

- **r2 修订来源**：用户明确交办的执行提示词（2026-09-21），内容为 **D1 解释返修与 CPU 数值稳健性补充**。
  本修订只修正 r1 的解释口径并补做 CPU 线性 probe 的数值稳健性检查；
  **不重跑编码、不重跑干预、不修改 r1 的任何数值产物**。
- **r2 授权范围**（严格限定）：必要的诊断代码、局部测试、文档修订，以及下面限定的 CPU probe。
  **GPU 使用、神经网络前向、optimizer updates、预训练与微调均为 0**；不启动 D2/D3/PH。
- **r2 新增预算**（与 r1 的 174 次分开计，不抵销）：四类 probe 与 residual probe 统一使用
  `alpha ∈ {0.01, 0.1, 1, 10, 100, 1000, 10000, 100000, 1000000}`（九点），
  上限为 4×9×6（四类 probe）＋ 4×6（OOF 与全 train 基线）＋ 9×6（residual）＝ **294 次真实数据 Ridge 拟合**；
  CPU probe 墙钟上限 60 分钟，失败与重跑均计入。**即使再次达到网格上界也不得继续扩大网格。**
- **r2 必须修正的解释口径**（保留旧数值，只改表述）：
  1. 删除“两路逐行余弦接近零 ⇒ 排除信息冗余”的结论：两路来自不同编码器，坐标系未对齐，逐行余弦不能判定信息独立；
  2. participation ratio ≈ 5 只说明**方差谱集中**，不等于只有五个非零方向或与任务相关的方向，也不单独证明 collapse；
  3. `alpha` 达到上界只说明**当前搜索范围可能限制结果**，不能提前断言负增量必然由正则化不足造成；
  4. “2D 逐位不变”更正为与实测一致的**“变化处于重复前向噪声量级”**；
  5. 本文件 r1 记录中的“九个开发单元”更正为实际的**六个**（`xc/eps/eat × fold0/1`）。
  上述修正**不得扩张为对 3D 有效或无效的结论**。
- **r2 不改变**：`L2/L3/L23/L23-S` 定义、置换 seed 与行对应规则、residual probe 定义
  （train 内三折 OOF、2D 基线 `alpha=1`、validation 用全 train 拟合的同一 `alpha=1` 基线）。
  **不加入 PCA、特征筛选、CKA、新模型或新的标准化方法。** 不访问 outer-test，不新增 `feat_finetuned` probe。

- 目标：定位现有 3D 相对 2D 的增量消失在哪里，再以最少的受控实验改善下游预测，重点关注 XC。
- 分工：Codex 规划与审查，ZCode 或用户指定模型执行。用户本轮授权编写本文件，不等于授权以下全部 GPU 预算。
- 规划基线：`dev@63a2e13`。编写前 ff-only pull 成功；存在用户删除 `Plan.md`、未跟踪 `.zcodeignore`，均保留。此前 PH 的 P0/P1 实现已进入最新提交，不因搁置而删除或宣称其已验收。
- 本轮按用户明确指定使用 **3D.md** 交接，不擅自恢复被删除的 `Plan.md`。执行记录追加本文件；周期审查后归档到 `PROJECT_HISTORY.md`。
- **PH 暂时搁置**：不启动 PH 后续预训练、融合实验或重跑。已有 PH 产物、代码和失败记录保留。若仍有活动任务，先与用户协调安全停止点，不擅自杀进程。
- 首批建议只授权 **D0＋D1**；其余阶段在审查前一阶段证据后单独授权。不得把整个计划当成一次性实验授权。

## 1. 现状、证据与待验证假设

### 1.1 固定参考，不混合不同 GLT 路线

本计划的主参考是 **S5 B_FP、concat、无 CLS、step 5000**，不是旧 N+1/N+2 蒸馏，也不是 GALPH 修复版 C1。

| 项目 | 本计划参考 |
| --- | --- |
| 配置 | `configs/mts/glt_pred_s3b_b_fp.json` |
| 部署包 | `results/glt_pred_20260918/s3b_formal/b_fp/pretrain/deploy_05000.pt` |
| 正式结果 | `results/glt_pred_20260918/s5_formal/b_fp/` |
| S5 审查 | `results/glt_pred_20260918/s5_formal_review/report.md` |
| 历史 O8 对照 | `results/glt_sci_o8ctrl_20260917/final_comparison/summary.json` |
| 下游 split | `data/splits/mips_outer5_inner20` |
| 下游 cohort | `data/processed/glt_dual_v2/downstream/cohort_1545eda5a8f6a1` |
| 下游几何／静态缓存 | `data/processed/mips_trimer_scage_downstream` ／ `data/processed/glt_dual_v2/downstream/dual_static_v1` |

执行时从实际 run/config/部署 metadata 核验这些路径和模型设置；缺失或矛盾时停止相应阶段，不找一个名字类似的包替代。复用已有身份校验，不另造版本管理框架。

历史 full 与同一双路包抽取 O8 的 macro7 R² 差约 **+0.000882**，但 EAT 约 +0.0120、EI 约 +0.0211，EPS 约 −0.0160、XC 约 −0.0104。因此现有证据是**任务间收益抵消、XC 不佳**，不是所有任务都没有任何 3D 信息。该历史结果不替代当前 S5 的 matched 对照。S5 的 B_FP/B_NONE 都有 3D，只回答 FP 的问题。

FGR、ALIGN、ENV、TORSION 和 PH retention 未建立稳定收益；这些方向不自动重启。修复 C1 的 PH encoder 可区分输入，也不证明其主干优于 S5 B_FP。

### 1.2 当前科学定义与主要瓶颈

以 `src/modules/glt_dual.py`、`glt_dual_pretrain.py`、`src/dataset/glt_dual_pretrain.py` 和实际配置为准：

- 2D：单 RU 的 O8 化学／周期路径输入，512 维、6 层；其单 RU 不等于没有跨 RU 拓扑信息。
- 3D：真实冻结开放 Trimer 的物理键 token；元素、键类型、键长进入 token，角度进入路径 attention bias；512 维、6 层，物理 line-hop ≤2。输出是中心内部键均值，不是独立原子空间图。
- 融合：两路各 512 维，经各自 LayerNorm 后 concat 成 1024 维，属性 head 输出标量。无效 3D 的处理沿用实际实现。
- 主预训练：masked-atom CE、带噪坐标下的中心键长／角度重建、7-RU FP BCE，权重 `[1,1,0.1]`。本轮不先删 FP，也不切换到 GALPH 的 masked-bond／CL 配方。
- 几何来源：单个通过检查的冻结 Trimer，不是最低能量构象集合、无限链或体相结构。局部几何可高度冗余于化学拓扑；非键接触与构象分布未被充分表示。

按以下顺序排查，不预先认定任何一项就是根因：

| 假设 | 证据应如何取得 | 对应阶段 |
| --- | --- | --- |
| 数据身份、几何有效性或读出处理有缺陷 | sample/atom/bond 对齐与 valid/N=0 检查 | D0、D1 |
| 预训练 3D 有增量，但融合／微调丢失 | 冻结表示 probe、微调前后表示与预测干预 | D1、D2 |
| 3D 只重复局部化学，缺少空间接触 | 表示响应检查、相同局部几何但不同空间结构 fixture | D1、D3 |
| 单 Trimer 与 XC 所需的信息尺度不匹配 | 有效实现仍无增量，结合标签和来源审计 | D0、阶段总结 |

门控很小、loss 下降、attention 大小都不能单独确认上述假设。增加层数、参数或训练步数也不是默认解决方案。

## 2. 共同实验合同

1. 开发范围固定 `xc/eps/eat × fold 0/1`，seed=42；保留既有 `outer5_inner20` manifest。训练仅用 train，选择仅用 validation，**不访问 outer-test 的标签、预测或特征进行诊断／选型**。可读取 split 索引做集合检查。
2. 数据拟合的 scaler、常量、分组统计只来自相应 train 或明确的预训练 P_train；不能由 validation/test 拟合。三组任务分别处理，不跨任务混合标签。
3. FULL adaptation、MSE、batch=32、eval batch=64、patience=10、warmup=5 epochs。开发最多 30 epochs，所有同阶段组使用相同 schedule 总长度。不能把 30-epoch 结果与历史 100-epoch 分数作受控归因。
4. 未明确改变的项沿用参考配置：O8 lr=1e-5、GLT lr=1e-5、其余 lr=1e-4、AdamW、微调 weight_decay=0.02。D2 仅改变指定参数组，不顺带修改 norm/bias decay、损失、mask 或数据。
5. 共享参数按名称复制同一初始张量；记录样本顺序及公共 RNG。新增模块初始化和 dropout 不得改变 O8／原 GLT 的公共随机流。不能只写“同 seed”就认为匹配。
6. 不因几何有效性筛除下游样本。中心内部键数为 0 时 3D 中心读出／几何监督为空且有限，学生原子路径仍参与；不把跨键改称中心监督。
7. 所有结果区分实现、smoke、开发和正式。已反复使用的开发数据不是独立盲测；fold 也不是独立重复实验。
8. 日志／结果根：`logs/glt_3d_gain_20260921/`、`results/glt_3d_gain_20260921/`。已有目录不覆盖；恢复只使用现有明确的 resume 规则。失败消耗和重跑都计入预算。

## 3. D0：执行前核对与静态审计

**预算：0 模型前向、0 optimizer updates、0 微调 epochs。**

### 3.1 执行前核对

- 读取 AGENTS.md、本文件、PIPELINE.md；检查 status、分支、remote，安全 pull 后重读。保护用户改动和最新 PH 实现。
- 检查 `tmux Uni-Poly` 和训练进程，记录已有任务；不得与 PH 或其他执行者同时写同一代码／产物。
- 明确主参考包、各 fold 旧 best checkpoint、config、split 与实际源码。旧正式模型只用于 train/validation 的机制诊断，不能再读取其 test 指标进行本轮选型。
- 记录当前与规划基线的差异；若主参考数学或数据定义已经变更，交回规划，不静默换基线。

### 3.2 必交的审计表

1. 预训练／微调各参数组：名称、数量、lr、decay、是否从预训练加载。重点核实 concat 的 `norm2/norm3` 是否确实被放到高学习率兜底组。
2. 2D/3D/fusion/head 的输入输出、loss 到参数的路径；明确 FP 是否更新融合归一化参数、部署属性 head 是否新初始化。
3. 下游各 task/fold train/validation 数量、几何 invalid 数、中心键数为 0 数、重复结构及标签分布；只列现有来源，XC 单位／含义／缺失实验条件不能靠名字猜测。
4. 现有缓存映射与氢处理：中心／两侧 RU、物理键、跨键、中心 mask；端帽与立体化学遗留审计问题单列，不用“输入有限”代替化学正确。
5. 将 PH 旧目标排列疑点单独登记：dataset 的 channel-first masked patches 与 head 的 patch-first 输出是否一致。该问题不用于解释无 PH 的 S5，本计划**不修复或重训 PH**。

产物 `d0/audit.md`，附实际源码位置和未核实项。源数据或身份合同错误阻断后续；普通信息缺失明确列出。

## 4. D1：零更新诊断——先判断 3D 是否包含可用增量

本阶段可以新增小型诊断代码和对应 fixture 测试，不修改训练数学。**无神经网络参数更新。** 线性 probe 的 CPU 拟合单独计入下述预算，不宣称完全没有拟合。

### 4.1 冻结表示与输入响应

在 B_FP 部署包 eval 模式提取六个开发单元 train/validation 的：

```text
sample_key / row_index / split
z2 = norm2(graph_2d)                  [512]
z3 = norm3(graph_3d), invalid后置零    [512]
z23 = concat(z2,z3)                   [1024]
geometry_valid / center_bond_count
```

相同结构可复用确定性编码，但性质行和标签不能去重。部署包中未经下游训练的随机属性 head **不能用来判断预测敏感性**；预测干预必须加载该任务／fold 已有 S5 best checkpoint，且仅在其 train/validation 样本上进行。

从 XC fold0 的 train 按固定 key 顺序取最多 256 个样本作为探针，不挑容易出现变化的样本。每个条件使用独立 batch 视图，禁止原地污染；保持状态／门控／RNG，记录 dtype。以 FP32 为主要诊断口径，同输入重复三次报告观测差异，不把其当严格误差上界。

干预条件：

- 原输入；关闭 3D 融合输入（归一化之后置零）；batch 内固定无自配置换 z3（样本数不足 2 时不计算）。
- 仅改 3D 的长度、仅改角度：将合法输入分别换为训练样本按 bond type／path type 统计的均值；稀有组退回 train 全局均值。统计限定 XC fold0 train，不能改拓扑、元素或标签。
- 均值干预不是物理构象，也不是新的训练数据；只用于管线响应检查。原始坐标／缓存不写回。

分别记录 token、graph、融合表示、最终预测的归一化变化及同输入重复地板。张量变化定义为逐样本 `||changed-original||₂ / max(||original||₂,1e-8)`，报告均值、中位数、最大值及未归一化差；标量预测同时报告原单位绝对差。只有实际覆盖完整 validation 的条件才报告该集合 R²；256 条子集不冒充完整 validation。推理消融属于分布改变下的敏感性检查，**不能替代重训的 2D-only 对照**。

比较旧 S5 best 与预训练部署的 z2/z3 方差、有效秩、余弦相似性，以及 train/validation 差异；不得凭秩小或两路相关就认定无用。

### 4.2 冻结线性 probe

六个开发单元分别训练以下 Ridge，`alpha ∈ {0.01,0.1,1,10,100}`，feature scaler 和 label scaler 只拟合 train，用 validation 选 alpha；R² 在原标签单位计算：

| probe | 输入 | 作用 |
| --- | --- | --- |
| L2 | z2 | 冻结 2D 基线 |
| L3 | z3 | 单独 3D 可读性 |
| L23 | z2＋z3 | 联合增量 |
| L23-S | z2＋固定置换 z3 | 样本对应关系诊断 |

置换分别在 train 和 validation 内，seed=`42+fold`，不跨 split、不参考标签。该控制不是严格的“相同分布无几何”模型；其失败不能单独证明几何有效。共 **120 次 Ridge 拟合**。

再做一次固定定义的残差 probe，避免只比较拼接维数：train 内 KFold(3, shuffle=True, random_state=42+fold)，用 alpha=1 的 2D Ridge 得到 OOF 预测，形成原标签单位残差；内折 scaler 仅拟合内折 train。用 z3 拟合残差，alpha 仍为上述五值；validation 预测为“全 train 拟合的 alpha=1 的 2D Ridge＋3D 残差预测”。与**同一个 alpha=1 基线**比较，不与另一个最佳 alpha 的模型混作差值。共 18 个内折基线＋6 个全 train 基线＋30 个残差模型＝**54 次拟合**。

### 4.3 预算、交付与决策

- 最多六个开发单元 train/validation 的编码、最多 256 个干预 probe，0 神经网络 updates；CPU Ridge 合计最多 **174 fits**。
- 使用单 GPU、DataLoader workers=0；编码／探针 GPU 墙钟上限 60 分钟，CPU probe 上限 60 分钟。到限保留中间产物并报告，不自动扩大。预计超过一分钟的命令在独立 tmux window 留日志。
- 独立测试只覆盖 batch 不变、顺序独立、状态恢复、scaler 无泄漏、中心 N=0 和分母；不重跑全仓测试。
- 交付 `d1/probes.json`、`d1/interventions.json`、`d1/report.md`，包含每折值、差值、训练拟合过程和实际消耗。

决策顺序：

1. 映射错、有效输入被忽略、NaN、错误 checkpoint：**STOP，先局部返修**，不启动架构比较。
2. L23−L2 或残差增量的 macro3 ≥0.005，且 XC 两折增量均为正：优先申请 **D2**，检验微调是否损失已有信息。
3. 未达上述条件但几何编码确有响应：只说明当前 probe 未建立可读增量；可申请 **D3** 检验非键空间信息，不能直接断言 3D 不含信息。
4. 表示明显变化、最终预测几乎不变：属于读出利用不足线索；结合 probe 决定 D2，不凭单项消融强制开门。

阈值仅用于工程分支选择，非统计显著性标准。Codex 审查后锁定下一阶段，不同时启动 D2 和 D3。

## 5. D2：最小微调优化，不改 3D 架构或预训练

科学问题：预训练 3D 已有可读信息时，预训练融合归一化／3D 编码器的更新策略是否导致负迁移？

四臂均从同一个 B_FP step5000 包初始化：

| arm | 唯一规定变化 |
| --- | --- |
| F2D | 只运行 O8；1024 维 head 输入为 `[norm2(z2), zeros(512)]`，不读取几何 |
| FBASE | 当前双路 FULL 原配置 |
| FNORM | FBASE 基础上，预训练加载的 norm2/norm3 lr 从 1e-4 改为 1e-5 |
| FSTABLE | FNORM 基础上，仅 GLT lr 改为 3e-6 |

F2D 与 FBASE 的共享 head 张量完全相同，head 结构相同；F2D 的零输入对应列不产生有效容量，须如实报告 active 参数量。F2D 回答“同一双路预训练包的 2D 是否足够”，**不等于独立 2D-only 预训练**。不得把现成旧 O8-only 分数填进本表。

FNORM−FBASE 检验参数分组；FSTABLE−FNORM 检验较小 GLT 学习率。无 CLS 的主参考不存在 CLS 学习率消融。不得同时改 weight decay、Huber、冻结／解冻、readout 或 2D learning rate。

实现验证预算：四臂 XC/fold0 各 1 epoch，共 **4 epochs**；检查 F2D 删除几何资源仍可推理、参数分组完整且不重叠、共同初始权重一致、所有预测有限。通过后交回；开发阶段另授权 **4×3×2＝24 units，最多 720 epochs**，不因 smoke 消耗抵销开发额度。

候选晋级须同时满足：相对 FBASE 和 F2D 的 macro3 ≥+0.005；XC mean ≥+0.01 且 XC 两折均为正；任一任务两折均值不下降超过 0.01。未达 gate 不追加 lr sweep。若只在非 XC 任务改善，记录任务特异证据，不能宣称 XC 优化成功。

若成功，优先确认这一最小改动，不再自动启动 D3；若失败，审查诊断后才决定 D3 是否仍有新假设。

## 6. D3：空间接触增强，保留中心 RU 读出

### 6.1 科学问题和边界

当前键长／键角不能唯一决定非键接触；沿单键改变二面角可以改变空间距离而保持部分局部键几何不变。新方案让 GLT 在中心读出前接收真实空间邻居消息，**不是再给末端 graph 向量叠一个易关闭门控**。

本轮仍使用现有物理 Trimer 状态，不混入 canonical N/N+1/N+2 状态共享改造；不声称无限周期几何。左右 RU 的状态可以更新，最终仍读中心内部键。这样只测试空间消息是否补足输入，不同时改变读出和状态身份。

### 6.2 输入与模块合同

保留原 GLT 化学 line attention、键 token、角度 bias、中心 mask 和全部 2D 输入。新增字段由实际映射获得，不从 RDKit 原子顺序猜测：

```text
physical_atom_pos       [A,3]        # 当前 forward 的坐标
physical_bond_ends      [B,2]        # 与 GLT B 个物理 bond states 严格对应
physical_atom_batch    [A]
physical_chemical_edge [2,Echem]    # 双向，排除 exact self
```

A 为当前 GLT token 所覆盖真实物理键的端点集合；原 GLT 若包含 H，则同步包含这些 H，不擅自切换重原子模型。孤立且无物理键的原子不会凭空创建中心键监督；原 2D 路径照常。

在 GLT 第 **3、5 层之后**各插入一个独立小模块，输入仍是该层全部物理键状态 H，宽 512：

1. 每个物理原子聚合 incident bond states 的均值，Linear(512,128)＋LayerNorm 得到 U；度为零返回零，禁止空 mean。
2. 对有向邻接 `(a,b)`（b 向 a 发送消息），用 `[U_a,U_b,RBF(d_ab),bonded_bit]`（289 维）输入 `Linear(289,256)→GELU→Linear(256,128)`；RBF 为 32 个中心，0–4 Å 等距，宽度 `σ=4/31 Å`，第 k 项为 `exp(-0.5*((d-c_k)/σ)^2)`，超过中心范围不截断距离；无 dropout。
3. 对同一 target a 的邻居消息取均值，得到 M_a；无邻居返回零。逐图计算，排除 exact self，禁止跨 batch 或合并不同物理副本。
4. 对物理键 `(a,b)`，更新 `H_ab ← H_ab + 0.1 × Linear(128,512)(M_a+M_b)`。输出投影无 bias；其余 Linear bias 初始化为零、权重 Xavier uniform。两处模块分别初始化且三臂共有部分按名称复制。

这里 **0.1 是固定工程起点，不是可学习零门控**；不宣称最优。端点和聚合对称，纯距离输入保持平移／旋转／反射不变；不是等变向量模型。

输出保持 `bond_states[B,512]`、`center_bond_states[N,512]`、`graph_3d[G,512]` 及 geometry_valid，concat/head 不变。中心 N=0 时沿用无效中心读出，不用侧 RU 均值伪造目标。

### 6.3 三臂匹配架构与预训练

| arm | 空间模块 | 邻接 | 归因 |
| --- | --- | --- | --- |
| GBASE | 无 | 原 GLT | 同预算重训基线 |
| GCHEM | 上述两处模块 | 真实化学键双向邻接 | 新容量／incidence 消息控制 |
| GSPACE | 与 GCHEM 参数完全相同 | 当前距离 ≤4 Å 的所有物理原子对 | 空间邻接整体增量 |

GCHEM/GSPACE 都读取真实距离和相同 RBF；差别是邻接范围，不是有无全部 3D。GSPACE−GCHEM 同时包含非键邻居及邻居数量变化，不称“纯距离值效应”。不加 TopK，不任意截断；按图分块精确计算距离，出现异常大图资源不足则停止报告，不静默改变邻域。

预训练用**本次 noisy 坐标**同时重算距离与空间邻接；微调用冻结 clean 坐标。不得把 clean 空间邻接预计算后作为 noisy view 的固定输入。原静态化学路径仍复用。原几何目标依旧来自 clean 坐标，属于条件去噪，不声称完全无几何线索。

三臂共同配方：

```yaml
pretrain:
  common_init: results/glt_pred_20260918/s3b_prep/common_init_v1.pt
  split: results/glt_pred_20260918/s3b_prep/pretrain_split_v1.json
  split_name: train
  optimizer: AdamW
  learning_rate: 0.0002
  weight_decay: 0
  precision: bf16
  world_size: 4
  microbatch_per_rank: 84
  accumulation: 3
  global_batch: 1008
  seed: 42
  max_updates: 5000
  warmup_updates: 2000
  schedule_total_updates: 20000
  end_learning_rate: 0.000000001
  atom_mask_ratio: 0.30
  noise_sigma: 0.03
  losses: [masked_atom_ce, center_geometry_reconstruction, fp_bce]
  weights: [1.0, 1.0, 0.1]
  geometry_head_norm: true
  third_task: fp
  use_md200: false
```

从共同初始状态重新训练，不把新空间模块接到已训 5k GLT 后当成 matched pretraining。共享的 mask、噪声、数据顺序与原网络 dropout 流一致；两处新模块初始化用独立 RNG。配置与源码无法精确复用时先交回，不擅自“近似公平”。5000 为固定导出步，不挑 validation 最优预训练步。

微调沿用 §2 的 FBASE 分组，不混入未确认的 D2 配方。新空间模块归入 GLT 编码器，lr=1e-5。比较 GBASE、GCHEM、GSPACE，另从**新 GBASE 同一个包抽取 F2D**，共四臂；不能复用另一条预训练轨迹的 F2D 作严格控制。

### 6.4 必要测试与有界预算

- 物理 incidence 对齐、重编号、RU 副本多重性、无邻居和中心 N=0；几何 invalid 在进入距离计算前跳过。
- 平移／旋转／反射不变，端点交换一致；截止边界专门检查，不把邻接变化误认为编码不变性错误。
- 确定性合成四原子链：改变二面角，中心键长／角度保持而非键距离变化；证明空间输入确实变化，不要求随机网络对所有 fixture 必须产生大响应。
- noisy 坐标驱动邻接和距离，clean 目标未混入输入；旧 cache 无写入。
- GCHEM/GSPACE 共有初始参数相同；非退化 fixture 下空间模块首步梯度有限且存在非零项；不要求所有参数每步非零。
- 部分 rank／全部 rank 无几何目标时 backward 和 collective 正常；使用现有 DDP 检查，不新增通用调度框架。
- 部署 strict-load、推理字段齐全；结果先落盘再做附加诊断，失败不留伪 PASS。

预算分开申请：

| 子阶段 | 上限 |
| --- | --- |
| correctness／resume | 每臂 2 步路径检查＋连续4步和2步恢复至4步，共10 updates；三臂合计30 |
| 微调 smoke | 四臂 XC/fold0 各1 epoch，合计4 |
| 正式预训练轨迹 | 三臂各5000，共15000 updates |
| 开发微调 | 四臂×三任务×两折＝24 units，最多720 epochs |

smoke 与正式轨迹分目录；正式不得从 smoke 权重继续。记录峰值显存、时间、参数量；额外计算是方案成本，不靠减少样本／邻居／更新数制造匹配。若开销超过 GBASE 的两倍，交回成本审查，不直接扩 GPU 或预算。

GSPACE 晋级须相对 GBASE、GCHEM 和 F2D 均满足 macro3 ≥+0.005、XC mean ≥+0.01 且 XC 两折均为正；任何任务 mean 不退化超过 0.01。若 GCHEM 有效、GSPACE 无额外收益，结论是支持新消息模块而非非键空间假设；可单列候选交回，不给 GSPACE 记成功。

## 7. 后续确认与当前明确不做的优化

D2 或 D3 最多选一个候选。开发 gate 通过不自动进入全量训练；Codex 先审查逐折历史、是否撞 epoch 上限、初始化及公共 RNG 证据，然后另写确认修订。

下一轮确认的预留设计是候选／对应双路基线／对应抽取 2D 共三组，XC/EPS/EAT × fold0/1 × seeds42/43/44、最多100 epochs：**54 units、5400 epochs 上限**。这只是待授权设计，不包含在本轮预算。之后才考虑八任务五折三组正式比较（120 units、最多12000 epochs，另行授权）。原开发 run 不能冒充此 100-epoch 确认。

不得根据 outer-test 调整候选、seed 或 checkpoint；不在 train＋validation refit。外层数据已有开发历史，最终仍不能称全新独立盲测。逐任务、逐折和 seed 变异分别报告，不将所有运行当独立数据点。

本轮不执行以下事项：

- PH 恢复、换 PH encoder、PH reconstruction 修复或任何 PH 消融。
- 重新生成 Trimer、多构象、P5、MD、力场优化、坐标 refinement 或缓存全量重建。
- 同时换原子空间 Transformer、共享 canonical 状态、CLS／pooling、2D attention scale 或损失目标。
- 因没有收益自动延长至20k、增加层数／宽度、换 seed 或做超参数 sweep。

若当前输入与结构正确但 D1/D2/D3 都无稳定增量，接受限定 STOP：**在当前单冻结 Trimer 与输入协议下，尚未建立额外预测价值**。下一步优先审计 XC 标签、立构与实验条件覆盖，或另立更丰富几何来源的计划；不能把结论扩张为“所有 3D 都无效”。

## 8. 实施范围、交付与停止条件

复用现有 dataset、collate、split、训练 runner 和保存逻辑；以下为拟新增接口，不是已有可直接运行命令：

```text
scripts/diagnose_glt_3d_gain.py            # D1 特征、干预与 probe
src/modules/glt_spatial_adapter.py       # 仅 D3 获授权后新增
configs/mts/glt_3d_gain/                  # 各授权阶段的显式配置
tests/test_glt_3d_gain.py                 # 只测本轮可能破坏的行为
```

D2 尽量只扩展现有 optimizer 分组和 F2D 路径；D3 使用独立显式架构选项，不改变旧模型默认行为。无需新建通用 registry、缓存迁移器或复杂 schema。允许复用已提交 PH 实现中的纯空间工具，但必须移除 PH 依赖并证明符合本计划数学，不能继承其训练配方冒充本方案。

每阶段交付：实际改动、命令／config、执行 commit、checkpoint 来源、split 身份、按臂／单元消耗、失败与重试、逐折结果、成本、未执行项；复用已有 run/metrics/runtime 结构，只有必要新字段才扩展。所有长任务位于 `tmux Uni-Poly` 独立 window，日志记录真实退出码。

立即停止受影响阶段的条件：映射或 key 不匹配、teacher/sidecar/部署来源混用、NaN/Inf、空集合均值、不同组使用不同任务样本、访问 outer-test、旧缓存写入、公共初始化不匹配、报告失败却标 PASS、需要覆盖产物或超预算。可以继续只读定位；不得靠静默丢样本、改协议或追加运行绕过。

执行者不能自行宣布科学验收。先在本文件追加执行记录，再交 Codex 审查。预算耗尽或阴性结果是合法停止，不是必须通过追加实验消除的失败。

## 9. 当前授权、首批交接与记录区

**已授权并完成：本文件规划、D0 静态审计、D1 零更新诊断、r2 D1 解释返修与 CPU 数值稳健性补充、
r3 D2 四臂实现与 smoke、r4 D2 公共随机流与运行记录返修、r5 split 契约收口。**
**D2 开发阶段（24 units）、D3、PH 与任何预训练仍未授权、未启动。**

### r5 授权范围（用户明确交办，2026-09-21）

**仅授权**：`resolve_fold` 的 split 校验返修、最小 CPU 合成测试、六个真实开发单元 manifest 的只读校验、
文档更新。
**GPU、模型前向、backward、optimizer updates、微调 epochs 全部为 0**；不启动 24 units、D3、PH、新 probe；
不重建、重写或替换现有 manifest；保留全部历史产物。
完成后状态为“**D2 实现收口完成，待 Codex 审查及开发阶段授权**”，不再追加 GPU 验证。

r5 交付物：`logs/glt_3d_gain_20260921/r5_split_check.log`、本文件 §9 的 r5 记录。

### r4 授权范围（用户明确交办，2026-09-21）

**仅授权**：相关代码、文档与必要有界验证（CPU 合成测试、≤4 臂×2 小 batch 的 train-mode
forward/backward、launcher 与记录返修）。
**不授权**：24 个 development units、预训练、PH、D3、新 probe；**不重跑** r3 的四个完整 epoch。
要求：登记 r4 时保留 r3 的 4 epochs / 36 updates 消耗；先记“**D2 需返修**”，
不得把 r3 smoke 记为公平性验收通过。完成后状态为“**D2 r4 返修完成，待 Codex 审查**”。

r4 交付物：`results/glt_3d_gain_20260921/d2_r4/`、`logs/glt_3d_gain_20260921/r4_*.log`、本文件 §9 的 r4 记录。

### r3 授权范围（用户明确交办，2026-09-21）

**仅授权**：文档与局部检查返修；**D2 四臂实现及必要局部测试**；
四臂 `XC/fold0` 各至多 **1 epoch**，合计至多 **4 epochs**（smoke）。
**不授权**：24 个开发 units、D3、PH、预训练、新 probe、额外 seed。
完成后交回 Codex 审查，**不自动晋级**。

r3 交付物：`results/glt_3d_gain_20260921/d2_smoke/`、对应日志目录、本文件 §9 的 r3 记录。

### 执行记录（由执行者填写）

**状态：D0/D1 执行完成，待 Codex 审查。** 执行者 ZCode，2026-09-21 UTC。

#### 执行前核对（3D.md §3.1）

- 读取 `AGENTS.md` 与本文件；分支 `dev`，`git status` 干净，`git pull --ff-only origin dev` 为 no-op。
- 规划基线 `dev@63a2e13`，本轮基线 `543710e`；其间仅有 `c71e1b7`（新增本文件）与 `543710e`（删除 `Plan.md`）两个文档 commit，
  `src/`、`configs/`、`scripts/`、参考产物未变，**未静默替换参考模型**。
- `tmux Uni-Poly` 的 73 个 window 全为历史任务，GPU 空闲、无训练进程；**未发现 PH 运行，未启动 PH 相关工作，未杀任何进程**。
- 未恢复被删除的 `Plan.md`，未改动无关文件。

#### 实际代码修改（本轮新增，未修改既有实现）

| 文件 | 作用 |
| --- | --- |
| `scripts/audit_glt_3d_gain_d0.py` | D0 数据审计（cohort/static 绑定校验 + 逐单元统计） |
| `scripts/audit_glt_3d_gain_params.py` | D0 参数组审计（复现 `optimizer_for` 分组与部署加载差异） |
| `scripts/diagnose_glt_3d_gain.py` | D1 编码／干预／probe 三个 mode |
| `scripts/analyze_glt_3d_gain_d1.py` | D1 表示统计（方差、有效秩、耦合、微调漂移） |
| `tests/test_glt_3d_gain.py` | D1 局部测试 11 项，`11 passed` |

未修改训练数学、未改动 `src/` 下任何实现、未新建或重建缓存、未覆盖既有产物。

#### 核验结果与证据路径

- **D0 五项审计**：`results/glt_3d_gain_20260921/d0/audit.md`（含已核实事实、实现疑点、未核实项三类清单）。
  - 已核实：`norm2/norm3` 确实落在高学习率兜底组 `fusion_head`（lr=1e-4）；部署包排除 `predictor.*`，
    新初始化张量恰为 `predictor.0.weight/bias`、`predictor.3.weight/bias`；FP 预训练分支经 `fuse()` 会更新 `norm2/norm3`。
  - 已核实：六个开发单元几何 invalid 全为 0；仅 xc 各折 train 各有 1 个“几何有效但中心键为 0”样本；
    三任务无重复结构；全 cohort 2610 条重复行只来自 egb/egc。
  - 已核实：3D 路线 heavy-only，token 不含显式立体化学字段；PH 的 channel-first / patch-first 排列不一致真实存在（只登记，未修复）。
  - 未核实：`xc` 标签单位与实验条件、`terminal_stereo_unset` 逐样本计数与坐标 E/Z 独立验证、
    历史 O8 对照数值、9 个无效结构的细分原因。
- **D1 表示与 probe**：`d1/report.md`、`d1/probes.json`、`d1/interventions.json`、`d1/representation.json`、
  `d1/feat_deploy/`、`d1/feat_finetuned/`、`d1/encode_deploy.json`、`d1/encode_finetuned.json`。
  `split_fuse` 与 `fuse()` 的等价性在运行时断言，实测 `max|delta| = 0.000e+00`。
- **局部测试**：`logs/glt_3d_gain_20260921/d1_tests2.log`，`11 passed`。

#### 六个开发单元的 probe 指标与配对差值

| 单元 | L2 | L3 | L23 | L23-S | L23−L2 | L23−L23-S | 残差增量（vs α=1 基线） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| xc/fold0 | 0.4038 | 0.1508 | 0.2745 | 0.2534 | −0.1292 | +0.0211 | −0.0516 |
| xc/fold1 | 0.3036 | 0.2680 | 0.3530 | 0.2410 | +0.0493 | +0.1120 | −0.0007 |
| eps/fold0 | 0.7175 | 0.5808 | 0.6919 | 0.7023 | −0.0255 | −0.0104 | −0.0270 |
| eps/fold1 | 0.7828 | 0.6635 | 0.7607 | 0.7297 | −0.0221 | +0.0311 | −0.0278 |
| eat/fold0 | 0.9720 | 0.9802 | 0.9840 | 0.9518 | +0.0121 | +0.0322 | +0.0033 |
| eat/fold1 | 0.9551 | 0.9699 | 0.9825 | 0.9395 | +0.0274 | +0.0430 | −0.0198 |
| **macro3** | — | — | — | — | **−0.01469** | **+0.03816** | **−0.02060** |

- `L23−L2` 在 4/6 单元为负，XC 两折不同号；残差增量 5/6 为负。**均未达到 D2 门控的 +0.005。**
- `L23−L23-S` 在 5/6 单元为正（macro3 +0.0382），提示样本级几何对应关系存在可读信号。
- **方法学限制**：`L23` 的 `alpha` 在 4/6 单元、残差 probe 在 6/6 单元撞到网格上界 100（`L2` 仅 1/6）。
  在 1024 维输入、244–276 个训练样本、participation-ratio ≈ 5（仅表示方差谱集中）的条件下，
  **搜索范围本身可能限制了结果**；这不等于负增量必然由正则化不足造成，也不等于“3D 无信息”。
  **本轮未扩大网格、未改变协议。**
- 表示统计：`z2` 与 `z3` 配对平均绝对余弦仅 0.024–0.046；两路有效秩均约 5，远低于 512 维；
  S5 相对部署包的 `z3` 漂移余弦 0.925–0.985。
  **〔r2 更正〕** 原 r1 表述为“近似正交，排除『3D 只是重复 2D』”，该推论已删除：
  两路来自不同编码器，坐标系未对齐，逐行余弦不能判定信息是否独立；此处只登记数值本身。

#### 干预响应与重复前向地板

- 探针：`xc/fold0` train 按 `sample_key` 升序取前 256/276 样本；统计量（距离按 `bond_type`、
  角度按 path token 类型对）只在 `xc/fold0` train 拟合（角度 102 组中 94 组有自身均值，8 组退回全局均值）。
- 相对变化（逐样本 `||Δ||₂/max(||x||₂,1e-8)` 的均值）：仅改长度时 3D token `0.1565`、`graph_3d` `0.1576`；
  仅改角度时 `0.2927` / `0.2977`。**2D 路径在所有 3D 干预下的变化处于重复前向噪声量级**
  （`1.3e-07`，与同路径重复地板同量级），可视为一条通过的健全性检查；
  **〔r2 更正〕** 原 r1 表述为“逐位不变”，与实测不符，已按上式更正（数值未变）。
- **重复前向地板**：表示层 `1.39e-07 – 1.47e-07`，预测 `4.2e-07 – 6.4e-07`；上述干预变化高出地板 6 个数量级以上。
- 解释限制：`no3d`（`z3=0`）是分布外输入，其预测变化不能读作“3D 信息量”；
  `permuted3d` 混合了对应关系破坏与扰动敏感性；`distance_mean`/`angle_mean` 是管线响应检查而非物理构象；
  **推理期消融不能替代 D2 的重训 2D-only 对照。**

#### 预算消耗、失败与重试

| 预算项 | 上限 | 实际 |
| --- | --- | --- |
| 神经网络 optimizer updates | 0 | 0 |
| Ridge 拟合次数 | ≤ 174 | 174 |
| 干预探针样本 | ≤ 256 | 256 |
| 编码／探针 GPU 墙钟 | ≤ 60 min | 约 3 min |
| CPU probe 墙钟 | ≤ 60 min | 约 0.5 min |
| DataLoader workers | 0 | 0 |

- 失败与重试（均计入预算，均未把部分产物标为 PASS）：
  1. D0 审计脚本首次运行把 static 的 `ordered_sample_key_hash` 误作 cohort 绑定参数，报 `binding mismatch`（退出码 1），
     修正为 `cohort_ordered_sample_key_hash` 检查后通过；
  2. D1 干预首次运行因 PyG `Data.to()` 为原地操作，条件循环把共享 CPU batch 移到 GPU，
     触发 CUDA→numpy 转换错误（退出码 1），改为每条件独立 CPU 副本后通过，该不变量已加入局部测试。
- 数值警告（非失败）：`sklearn` 发出 16 次 `LinAlgWarning: Ill-conditioned matrix`（`rcond ≈ 2.7e-08 – 5.4e-08`），
  对应被选中的非最优 `alpha`，结果未丢弃、未替代。
- 未执行项：无 2D-only 重训对照、未扩 `alpha` 网格、未访问 outer-test、未修改缓存或训练数学。

#### 决策建议

| 3D.md §4.3 规则 | 本轮结果 |
| --- | --- |
| 1 映射／NaN／错误 checkpoint → STOP 返修 | **不触发**（无 NaN/Inf；几何干预确实驱动表示；checkpoint 身份经断言） |
| 2 `L23−L2` 或残差 `macro3 ≥ 0.005` 且 XC 两折均正 → D2 | **不满足**（−0.0147 / −0.0206，XC 两折不同号） |
| 3 未达规则 2 但几何编码确有响应 → 可申请 D3 | **满足**（见干预响应） |
| 4 表示变化而预测几乎不变 → 读出利用不足 | **不适用**（预测同样明显变化） |

- 建议按规则 3 收尾：**在当前单冻结 Trimer、heavy-only、无显式立构 token 的协议下，
  冻结线性读出未建立 `z3` 的可读增量，同时几何输入确实改变表示**；该表述**不能扩张为“3D 不含信息”**。
- 在决定 D3 之前，建议 Codex 先裁定第 4 节的 `alpha` 饱和问题（返修 probe 设置 vs 直接授权 D3）。
  两者都超出本轮预算，**本轮未执行、未自行扩额、未晋级**；D2/D3/PH 均未启动。

### r2 执行记录：D1 解释返修与 CPU 数值稳健性补充

**状态：D1-r2 执行完成，待 Codex 审查。** 执行者 ZCode，2026-09-21 UTC。
**GPU 使用 0、神经网络前向 0、optimizer updates 0**；未重跑编码或干预；D2/D3/PH 未启动。

#### 授权与边界

- 用户明确交办的 r2 提示词；只授权必要的诊断代码、局部测试、文档修订与本轮限定的 CPU probe。
- 复用 `d1/feat_deploy/`，**未重新编码**；未使用 `feat_finetuned` 做新增 probe；未访问 outer-test。
- 新预算与 r1 的 174 次分开计：九点网格上限 **294 次**真实数据 Ridge 拟合，实际执行 **294**。
- **即使再次达到网格上界也不扩大网格**（已遵守：四类 probe 全部落在内部，残差 3/6 撞上界但未扩）。

#### 执行前核对与特征身份核验

- `pull --ff-only` no-op；工作区干净，`HEAD = 95e3185`，与 `origin/dev` 一致；`tmux Uni-Poly` 无活动任务。
- `scripts/verify_glt_3d_gain_d1_features.py` 对 12 个 `feat_deploy` 存档逐一核对
  **split 标签、行顺序、`sample_key`、`label`、有限性、形状与几何标记**，全部通过
  （`d1_r2/feature_identity.json`）；行数与 r1 一致。

#### 解释口径修正（保留全部旧数值）

| # | r1 原表述 | r2 更正 |
| --- | --- | --- |
| 1 | “两路近似正交 ⇒ 排除『3D 只是重复 2D』” | **删除该推论**：两路来自不同编码器，坐标系未对齐，逐行余弦不能判定信息独立；只登记余弦数值 |
| 2 | participation ratio 约 5 | 只说明**方差谱集中**，不等于只有五个非零或与任务相关的方向，也不单独证明 collapse |
| 3 | `alpha` 撞上界 ⇒ 负结论可能由正则化不足造成 | `alpha` 撞上界只说明**当前搜索范围可能限制结果**；不等于负增量必然由正则化不足造成 |
| 4 | “2D 逐位不变” | 更正为**“变化处于重复前向噪声量级”**（实测 `1.3e-07`，与同路径重复地板同量级） |
| 5 | “九个开发单元” | 更正为实际的**六个**（`xc/eps/eat × fold0/1`） |

`d1/report.md` 正文数值与 r1 产物**未修改**，仅在文首追加一条指向本节的 r2 更正指引。

#### r2 结果

九点网格（`0.01 … 1e6`）下的选中值与残差增量：

| 单元 | L2 | L3 | L23 | L23-S | 残差增量（基线 α=1） |
| --- | --- | --- | --- | --- | --- |
| xc/fold0 | 0.4038 @ 10 | 0.1508 @ 100 | 0.2805 @ 1000 | 0.2534 @ 100 | **+0.02156** @ 1000 |
| xc/fold1 | 0.3036 @ 100 | 0.2680 @ 10 | 0.3530 @ 100 | 0.2419 @ 1000 | **+0.00770** @ 10000 |
| eps/fold0 | 0.7175 @ 10 | 0.5808 @ 10 | 0.6919 @ 100 | 0.7023 @ 10 | +0.00086 @ 1e6⁺ |
| eps/fold1 | 0.7828 @ 10 | 0.6635 @ 10 | 0.7607 @ 100 | 0.7297 @ 100 | +0.00080 @ 1e6⁺ |
| eat/fold0 | 0.9720 @ 1 | 0.9802 @ 10 | 0.9840 @ 10 | 0.9518 @ 1 | +0.00333 @ 100 |
| eat/fold1 | 0.9551 @ 0.1 | 0.9699 @ 10 | 0.9825 @ 10 | 0.9395 @ 10 | +0.00022 @ 1e6⁺ |

⁺ 上界。配对差值（r2 九点）：

| 指标 | xc | eps | eat | macro3 |
| --- | --- | --- | --- | --- |
| `L23−L2` | −0.03697 | −0.02382 | +0.01973 | **−0.013685** |
| `L23−L23-S` | +0.06909 | +0.01033 | +0.03761 | **+0.039009** |
| 残差增量 | +0.01463 | +0.00083 | +0.00178 | **+0.005745** |

**三个影响的分开说明**：

| 影响 | 定义 | 残差 `macro3` 变化 |
| --- | --- | --- |
| 数值修正 | r1 → r2（同五点） | **−0.000005**（可忽略） |
| **网格扩大** | r2 五点 → r2 九点 | **+0.026348**（决定性） |
| 合并 | r1 → r2 九点 | +0.026343 |

- **边界命中**：r1 的“`L23` 4/6、残差 6/6 撞上界”在九点下消失——四类 probe **全部 interior**，残差 3/6 撞上界。
- **警告**：r2 运行 `Warning` 计数为 **0**（r1 有 16 次 `LinAlgWarning`）；数值修正只改变病态 `alpha` 的数值
  （该区域差异最大 `1.19e-01`），在所有被选中的 `alpha` 上差异 ≤ `1.3e-04`。

#### 门控判定（沿用原门槛，未事后降低）

| 协议 | 残差 macro3 | XC 两折均正 | 残差通过 | `L23−L2` macro3 | 通过 |
| --- | --- | --- | --- | --- | --- |
| r1（默认 solver，五点） | −0.020598 | 否 | 否 | −0.014685 | 否 |
| r2（SVD＋float64，五点） | −0.020603 | 否 | 否 | −0.014683 | 否 |
| **r2（SVD＋float64，九点）** | **+0.005745** | **是**（+0.02156 / +0.00770） | **是** | −0.013685 | 否 |

#### 预算、失败与未完成项

- 真实数据 Ridge 拟合 **294/294**（`L2/L3/L23/L23-S` 各 54、OOF 18、全 train 基线 6、残差 54），
  预算在**每次拟合前**检查；CPU 墙钟 **12.46 s**；BLAS 线程 `OMP/OPENBLAS/MKL/NUMEXPR=4`（已记录）。
- 局部测试 `tests/test_glt_3d_gain_r2.py`：**8 passed**（合成数据，不占用真实数据预算，不构建模型）。
- 失败与重试：本轮无失败运行；分析脚本首次运行后自查发现 `r2_five` 与 `r2_nine` 的残差增量误用同一份九点数值
  （分析代码缺陷），已修复并重跑分析，**不影响 `probes_r2.json`**。
- 未完成项：未扩大网格（指令封顶）；未做 2D-only 重训对照；未访问 outer-test。
- 报告数字已用脚本对 `probes_r2.json` / `analysis.json` 逐表核对（§3、§4.1、§4.2、边界表、§5 全部一致）。

#### 下一步建议

**建议申请 D2**（门槛已按原标准满足：残差增量 `macro3 = +0.005745 ≥ 0.005`，XC 两折均为正），
但必须连同以下限定一并审查：

1. 增量**高度集中在 XC**（逐任务 +0.01463 / +0.00083 / +0.00178，宏平均约 85% 来自 XC）；
   `eps` 两折与 `eat/fold1` 的最优 `alpha` 撞上界、增量本质接近“不做残差修正”，属平凡正值。
2. XC 的峰在 `alpha` 轴上是**内部峰**（`1e3`、`1e4`），但依赖约一个数量级的峰宽，**未扩大网格验证稳定性**。
3. 结论仍是**反复使用同一 validation** 的开发诊断（validation 仅 61–69 行，`alpha` 由该 validation 选出），
   不是性能提升、不是独立盲测、不是因果证据。
4. `L23−L2` 与 `L23−L23-S` 的对照仍提示拼接本身不带来增益；增量只出现在“2D 基线 ＋ 3D 残差”这一形式上。

若 Codex 认为该通过不足以支撑 D2，则退回到“当前稳健线性 probe 在主要形式上未建立增量”的表述，
是否进入 D3 同样交 Codex 裁定。**执行者不自动启动任何后续阶段。**

### r3 执行记录：D2 最小微调对照的实现与 smoke

**状态：D2 实现与 smoke 完成，待 Codex 审查。** 执行者 ZCode，2026-09-21 UTC。
**未启动 24 units、D3、PH、预训练或额外 seed**；产物见 `results/glt_3d_gain_20260921/d2_smoke/`。

#### 执行前核对

`git status` 干净，`HEAD = 563668f`（r2 提交），`git pull --ff-only origin dev` 为 no-op；
`tmux Uni-Poly` 无活动任务，GPU 空闲；未恢复 `Plan.md`，未改动无关文件。

#### 审查问题逐项回应

| # | 审查意见 | 处理 |
| --- | --- | --- |
| 1 | 保留 r2 门槛通过结论 | **未改门槛**，r2 原结论保留 |
| 2 | 明确残差增量针对固定 `alpha=1` 基线；补列 XC 基线／残差模型／最佳 L2；不宣称 3D 超过调参后 2D | `d1_r2/report.md` 新增第 6 条：`xc/fold0` 0.1219 / 0.1435 / **0.4038**；`xc/fold1` 0.0918 / 0.0995 / **0.3036**；明确写入“**不得**读作 3D 已超过调参后的 2D” |
| 3 | 更正“大 `alpha` 残差必然趋于零” | 已更正三处：`fit_intercept=True` + label scaler 下极限是**训练残差均值**（非零常数），非零；本轮未追加真实数据拟合 |
| 4 | 零 `z3` 检查漏掉 `geometry_valid=false` 行 | 抽成 `assert_zero_z3_rows()`，**分别**断言 `center_count==0` 与 `geometry_valid=false` 两类行；核验输出新增 `zero_z3_rows`；未重新编码 |
| 5 | 补直接覆盖生产 OOF 回填的测试 | 抽出 `cross_fitted_oof()` 并改写调用处；新增测试覆盖每行恰一次、内折索引不相交、与独立 replay 逐位一致、特征与标签独立时不可预测；未重跑 294 次真实 probe |

返修后回归：`tests/test_glt_3d_gain_r2.py` **11 passed**。

#### 修改文件

**新增**（未修改任何 `src/` 既有实现）：
`scripts/finetune_glt_3d_gain_d2.py`（四臂 runner）、`scripts/verify_glt_3d_gain_d2_smoke.py`（核验）、
`scripts/run_glt_3d_gain_d2_smoke.sh`（顺序启动器）、`tests/test_glt_3d_gain_d2.py`（**14 passed**）；
返修涉及 `scripts/verify_glt_3d_gain_d1_features.py`、`scripts/diagnose_glt_3d_gain_r2.py`、`tests/test_glt_3d_gain_r2.py`。

#### 配置与参数组证据

| arm | o8 | glt | norm2/norm3 | predictor | 组数 |
| --- | --- | --- | --- | --- | --- |
| `f2d` | 1e-5 | —（无该分支） | 1e-4 | 1e-4 | 3 |
| `fbase` | 1e-5 | 1e-5 | 1e-4 | 1e-4 | 4 |
| `fnorm` | 1e-5 | 1e-5 | **1e-5** | 1e-4 | 4 |
| `fstable` | 1e-5 | **3e-6** | **1e-5** | 1e-4 | 4 |

`optimizer_for_arm()` 对**重复分组**与**未分组张量**都直接抛错；`run_<arm>.json` 逐名称记录每组
`lr`/`weight_decay`/参数量。其余项沿用参考配置（`MSE`、`AdamW`、`wd=0.02`、`batch=32`、`eval_batch=64`、
seed 42、`outer5_inner20`、train-only scaler），未新增冻结／Huber／CLS／门控／readout 变化。

#### 公共初始化的实现与验证

- **实现**：`build_reference()` 以固定 `COMMON_INIT_SEED=42` 构建参考模型并载入 `B_FP` step5000 部署包；
  `build_arm()` **按名称**复制每个张量（臂中缺失来源的张量直接报错），四臂共享逐位相同的
  `o8`/`glt`/`norm2`/`norm3`/`predictor` 初始张量与同一个 head 抽样；F2D 不含 GLT，其初始化不消耗 GLT 随机流。
- **样本顺序**：`DataLoader(generator=torch.Generator().manual_seed(seed + fold))`，四臂同参。
- **验证**：核验脚本用相同 seed 与部署包**重建**参考模型与四臂初始状态，逐张量比对 ——
  **191/191 张量逐位一致，无 mismatch**。
- **限度**：`best.pt` 保存训练后状态，无法回溯当时初始值；“smoke 运行时用了同一初始化”由启动器代码路径保证，
  不由重建比对本身证明。
  **〔r4 更正〕** 原 r3 表述为“各臂训练期 dropout 的随机数消耗因结构差异而不同，**不属于公共随机流定义范围**”，
  该合同缩减**已删除**：训练期 dropout 正属于公共随机流，`f2d` 缺少 GLT 分支会使 GLT dropout 的消耗
  移动 O8/head 的抽样位置。r4 已在 D2 专用路径实现隔离（见本节 r4 记录），
  因此 **r3 smoke 的四臂并不满足公平性要求，不得作为公平验收依据**；其数值与产物保留未改。

#### 每臂实际执行与退出状态

| arm | 退出码 | epochs | updates | 训练步 lr（epoch 末） | best epoch | validation R² |
| --- | --- | --- | --- | --- | --- | --- |
| `f2d` | 0 | 1 | 9 | o8 2.22e-6, norm 2.22e-5, head 2.22e-5 | 1 | 0.09269 |
| `fbase` | 0 | 1 | 9 | o8 2.22e-6, glt 2.22e-6, norm 2.22e-5, head 2.22e-5 | 1 | 0.09239 |
| `fnorm` | 0 | 1 | 9 | o8 2.22e-6, glt 2.22e-6, **norm 2.22e-6**, head 2.22e-5 | 1 | 0.09241 |
| `fstable` | 0 | 1 | 9 | o8 2.22e-6, **glt 6.67e-7**, **norm 2.22e-6**, head 2.22e-5 | 1 | 0.09134 |

调度器按**原开发 30-epoch schedule** 构造（`total = 270` 步、`warmup = 45` 步），只提前停止，
**未把 schedule 总长度改为 1**。记录的 lr ＝ `base_lr × 10/45`，与各组基准 lr 一一吻合。
**四臂 R² 差异 ≤ 0.0014；〔r4 更正〕这是单 epoch 观察差异，缺少重复证据，不作性能解释**
（原 r3 表述“落在单轮噪声量级”暗示了未测的重复变异，已更正；数值未变）。
此外 r3 四臂并未共享同一条公共随机流（见上一条〔r4 更正〕），该表数值**不作为公平对照**。

#### checkpoint、有限性与 2D-only 路径证据

- **strict-load**：四臂 `best.pt` 全部 `missing_keys=[]`、`unexpected_keys=[]`。
- **参数确实更新**：`f2d` 89/89、其余三臂 191/191 张量均发生位移，无“未移动”张量。
- **有限性**：训练/验证 loss 与 R² 全部有限，无 NaN/Inf。
- **2D-only 路径**：测试删除全部 14 个几何字段后 `f2d` 仍能前向并给出有限预测；
  同一条件下的负对照证明 `fbase` 确实读取这些字段（抛 `AttributeError`）。
- **有效容量（如实报告）**：`f2d` 总参数 19,524,377，其中 **262,144** 死参数
  （`predictor.0` 接收结构零的 512×512），有效 **19,262,233**；其余三臂 39,547,255 全有效。
- **train-only scaler**：四臂 `scaler_mean = 37.348897` ＝ train 标签均值（validation 均值 34.577148，不匹配）。
- **分割**：train/validation/test 互不相交且合计等于 `sample_count`；metrics 记录 `outer_test: NOT_RUN`。

#### 预算、失败与未完成项

各臂 XC/fold0 各 1 epoch，**合计 4 epochs、36 个 optimizer updates**，与环境预算一致。
**无失败、无重试**（启动器在非零退出时会停止且不重跑，本轮未触发）。
**24 units 开发阶段、D3、PH、预训练、新 probe、额外 seed 均未启动。**

#### 结论

本轮只证明四臂**只差指定学习率**、**共享同一初始状态**、各自可训练可保存，且 **F2D 不依赖几何字段**。
D2 的候选判定需要 24 units 的 macro3 与 XC 两折证据，**本轮不具备也不宣称任何晋级结论**。

### r4 执行记录：D2 公共随机流与运行记录最小返修

**状态：D2 r4 返修完成，待 Codex 审查。** 执行者 ZCode，2026-09-21 UTC。
**未启动 24 units、预训练、PH、D3、新 probe**；**未重跑 r3 的四个完整 epoch**
（`d2_smoke/` 下 4 个单元产物与日志 mtime 保持在 02:42，未覆盖）；本轮 **optimizer updates = 0**。
详细报告：`results/glt_3d_gain_20260921/d2_r4/report.md`。

#### 执行前核对

`git status` 干净，`HEAD = eae7b81`（r3 提交），`git pull --ff-only origin dev` 为 no-op；
`tmux Uni-Poly` 无活动训练进程，GPU 无 compute app；未恢复 `Plan.md`，未改动无关文件。

#### 审查意见逐项回应

| # | 要求 | 处理 |
| --- | --- | --- |
| 二.1 | GLT 随机流与 O8/head 公共流隔离，异常时也恢复 | `IsolatedRandomStream` + `DualArm.encode`（仅 D2 脚本）；进入保存公共状态、退出保存私有状态并恢复公共状态，`__exit__` 在异常路径同样执行 |
| 二.2 | 不按 batch 重置 seed | 私有状态只播种一次，跨调用推进；GPU 证据 GLT 摘要 `90f9…`→`748e…`，entries 0→2 |
| 二.3 | 不关 dropout、不改概率/输入/数学/学习率 | 仅替换随机源；`ARM_LRS`、损失、mask、数据、schedule 未改 |
| 二.4 | 三双路臂共享 GLT 流；四臂共享 O8/head 流 | 同一 `GLT_STREAM_SEED=42`；GPU 上四臂公共流摘要逐 batch 完全相同 |
| 二.5 | 只在 D2 路径实现，不改历史 runner | 只改 `scripts/finetune_glt_3d_gain_d2.py`；`src/` 未改 |
| 二.6 | 删除“dropout 不属于公共随机流”的合同缩减 | 已删除并更正（见上文 r3 记录的〔r4 更正〕）；明确本修复会改变 r3 smoke 轨迹，旧产物保留 |
| 三 | 随机流测试用 train 模式、≥2 batch、GLT 额外汇耗、异常恢复、生产路径 | `tests/test_glt_3d_gain_d2.py` 新增 11 项（含负对照），文件内 25 passed |
| 四.1 | 失败立即以原退出码退出；仅全部成功写完成标志 | launcher `exit "$code"`，`ALL_ARMS_OK` 仅在全部成功后写 |
| 四.2 | 不截断旧日志、不覆盖已有单元产物 | 删除 `: > "$LOG"`；单元目录 `exist_ok=False` |
| 四.3 | run/runtime 入 arm/task/fold 目录并标记成功/失败 | `run.json`／`runtime.json` 写入单元目录；失败写 `status=FAILED` 并保留错误与退出码 |
| 四.4 | `executed_epochs` 用实际 history 长度；`requested_epochs` 另存；记录实际 updates | `run_epochs()` 返回真实 history；三项分别记录 |
| 四.5 | 区分 smoke/development；只实现 development 入口不运行 | `--stage {smoke,development}`＝1／30 epochs，task 限 `xc/eps/eat`、fold 限 `0/1` |
| 四.6 | 执行前校验 outer5_inner20 与三集合分离 | `resolve_fold()` 校验并记录 `outer_test: NOT_RUN`；只物化 train/validation |
| 四.7 | 保留 30 epoch schedule | `SCHEDULE_TOTAL_EPOCHS=30`、`SCHEDULE_WARMUP_EPOCHS=5` 未变 |

#### 修改文件

| 文件 | 变化 |
| --- | --- |
| `scripts/finetune_glt_3d_gain_d2.py` | 新增 `IsolatedRandomStream`、`ambient_rng_digest`、`DualArm`；新增 `unit_directory`／`prepare_unit_directory`／`resolve_fold`／`run_epochs`／`failure_record`；`--stage` 与 epoch/task/fold 限制；run/runtime 记录改造 |
| `scripts/run_glt_3d_gain_d2_smoke.sh` | 失败立即以原退出码退出；日志改为追加；仅全部成功写完成标志；launcher 参数可用环境变量覆盖（供合成测试使用） |
| `scripts/verify_glt_3d_gain_d2_rng.py` | **新增**：有界 GPU 随机流核验（4 臂×2 小 batch，`optimizer.step=0`），失败也落盘 |
| `tests/test_glt_3d_gain_d2.py` | 新增 11 项 r4 测试（随机流 4、记录与守卫 4、launcher 2、阶段限制 1 等） |

`src/`、`configs/`、缓存与 r3 产物均未修改。

#### 测试与验证命令

| 命令 | 结果 |
| --- | --- |
| `python3 -m pytest tests/test_glt_3d_gain_d2.py tests/test_glt_3d_gain_r2.py -q` | **36 passed**（D2 25、r2 11），无告警（除无关的 torch_geometric 弃用提示） |
| `python3 scripts/verify_glt_3d_gain_d2_smoke.py … --output d2_r4/verification_rerun.json` | **PASS**：191/191 共享张量一致，各臂 moved／R² 与 r3 相同 → r4 未破坏 r3 证据链 |
| CLI 守卫（`r4_cli_guards.log`） | smoke 2 epochs、development 31 epochs、`--task egb`、`--fold 2` 均被拒（退出码 1／1／2／2）；development 30 epochs 在 tmux 外停在 `require_tmux()`，未建目录、未启动训练 |
| launcher × 真实 runner（`r4_launcher_guard.log`） | 已存在单元目录 → `FileExistsError` → 退出码 1 → `ABORT after f2d`，后续 arm 未启动，无完成标志，原文件未动（检查用 `/tmp` 构造目录） |
| GPU 随机流核验（`r4_rng_gpu.log`、`d2_r4/rng_gpu.json`） | **10/10 检查通过**，退出码 0，3.1 s |

#### 随机流证据（xc/fold0 train 前 8 行，每臂 2 个 4 行小 batch，train 模式）

- 公共流（O8/head）摘要：四臂在 batch0 均为 `5d7a0623c65f43ed`、batch1 均为 `4609502470885ba7`，
  且两批不同 → 四臂共享公共流且逐 batch 推进。
- GLT 私有流摘要：三双路臂在 batch0 均为 `90f9be271a5c294a`、batch1 均为 `748e34539917caec`，
  两批不同、entries 各 0→2 → 三双路臂共享私有流且不按 batch 重放；`f2d` 无私有流。
- 异常路径：公共流恢复到仅跑 O8 的位置、私有 entries 仍推进（两项检查为 true）。
- 附带观测（不作验收条件）：未更新任何参数前三条双路臂预测逐位相同，`f2d` 不同；
  损失仅作有限性检查，不作性能比较。

#### 保存与失败行为

- 成功：单元目录内写 `run.json`、`metrics.json`、`runtime.json`（`status=PASS`、`exit_code=0`）与 `best.pt`，
  控制台输出 `status: PASS`。
- 失败：单元目录内写 `runtime.json`（`status=FAILED`、错误类型与消息、`exit_code=1`）后抛出，**不写 PASS**；
  目录已存在则 `FileExistsError` 且不触碰任何已有文件。
- launcher：非零退出立即 `exit "$code"` 并写 `ABORT … no rerun within this budget`，不写 `ALL_ARMS_OK`。

#### 预算、失败与未完成项

| 项 | 授权 | 实际 |
| --- | --- | --- |
| GPU train-mode forward+backward | ≤8（4 臂×2 小 batch） | **16**（尝试 1＝0、尝试 2＝8、尝试 3＝8）**超预算** |
| optimizer updates | 0 | 0 |
| 完整 epoch | 0 | 0 |
| r3 smoke 的 4 epochs／36 updates | 保留不重跑 | 未重跑 |

- 失败与重试（均如实记录，未把失败写入 PASS）：
  1. `verify_glt_3d_gain_d2_rng.py` 首次运行缺 `import numpy`，在读取数据阶段崩溃（退出码 1，**0 次模型调用**）；
  2. 第二次运行完成全部 8 次调用并把摘要打印进日志，但在**报告写入**阶段因预测变量遮蔽输出路径而崩溃
     （退出码 1，未写出 JSON）；修复后第三次运行退出码 0；
  3. 第三次运行的摘要与第二次**逐位相同**，故该重跑同时构成随机契约的重复性证据。
  **超预算事实不因代价小（4 行小 batch、约 3 秒）而改变**：本轮 GPU 前/反向调用为授权量的 2 倍，
  未追加 arm／batch 大小／task／fold／epoch，交由 Codex 裁定是否需返修或追认。
- 未完成项：development 入口（24 units）未运行；GPU 脚本的异常路径明细字符串未入本次 JSON
  （仅布尔检查，同一性质由 CPU 测试覆盖）；未访问 outer-test；未修改缓存或 `src/`。

#### 结论

本轮只证明四臂共享同一初始张量、同一 O8/head 公共流，三条双路臂共享同一 3D 私有流，
且失败、退出码与不覆盖规则可复现。**不产生预测性能结论，也不构成 D2 开发阶段授权**。
r3 smoke 的四个数值因缺少公共随机流隔离与重复证据，只作历史记录保留。

### r5 执行记录：split 契约收口

**状态：D2 实现收口完成，待 Codex 审查及开发阶段授权。** 执行者 ZCode，2026-09-21 UTC。
**GPU、optimizer updates、微调 epochs 全部为 0**（本轮只有 CPU 校验与合成测试）；
未启动 24 units、D3、PH、新 probe；未重建、重写或替换任何 manifest；全部历史产物保留。

**〔r6 更正〕** 上句的“模型前向”表述不准确，按已有命令、日志与测试代码更正如下：

- r5 **新增的 5 项** split 契约测试是纯 CPU 合成测试（小型 dict 与 json 副本），**不构建模型、不做前向**；
- 但 r5 的**完整 D2 回归**并不只有合成测试：`tests/test_glt_3d_gain_d2.py` 的
  `arms`／`xc_unit` fixture 会构建真实 20M 参数模型并在冻结的 `xc/fold0` fixture 上做前向
  （如 `test_fbase_matches_the_reference_forward`、`test_f2d_predicts_when_every_geometry_field_is_absent`、
  `test_centre_bond_zero_sample_keeps_a_finite_prediction` 等），这些前向在 r3 引入、r4 扩充、r5 一并复跑；
- 因此“模型前向为 0”这一说法**只适用于 r5 新增的 5 项合成测试，不适用于整套回归**；
  整套回归中的实际模型前向**次数未逐次计数，标记为未核实**。
- 这些前向是 CPU 上的 fixture 前向，**没有 optimizer update、没有训练 epoch、不使用 GPU**；
  但本轮不再宣称“模型前向为 0”。**不为补计数重新执行测试**（按 r6 授权）。

#### 执行前核对

`git status` 干净，`HEAD = 4d9f302`（r4 提交），`git pull --ff-only origin dev` 为 no-op；
未恢复 `Plan.md`，未改动无关文件。

#### r4 修复的技术自检结果（执行者证据，不是 Codex 验收）

| 项 | 证据 | 结论 |
| --- | --- | --- |
| 公共随机流隔离 | GPU 核验 10/10 检查通过（`d2_r4/rng_gpu.json`）：四臂公共流摘要逐 batch 相同、三双路臂私有流摘要相同且跨 batch 推进 | 技术上成立 |
| 异常路径恢复 | CPU 生产路径测试 + GPU 两项布尔检查为 true | 技术上成立 |
| 不按 batch 重放 | 私有流摘要跨 batch 不同、entries 0→2 | 技术上成立 |
| launcher 失败语义 | pytest 2 项 + 真实 runner 守卫检查：非零退出立即中止、不写完成标志、日志只追加 | 技术上成立 |
| 单元记录粒度 | `run.json`／`runtime.json` 落 `arm/task/fold` 目录，`requested/executed_epochs` 与 `optimizer_updates` 分开记录，失败写 `FAILED` | 技术上成立 |
| 不破坏历史证据 | r3 核验脚本在 r4 代码下复跑结果一致（191/191 共享张量，各臂 moved／R² 相同） | 技术上成立 |
| **超预算** | **GPU 前/反向调用实耗 16 次，原授权 8 次，超额 8 次**（两次报告路径缺陷各消耗一次完整运行，失败日志保留在 `r4_rng_gpu.log`） | **未获追认** |

**技术自检通过不等于用户追认超预算**：本轮不对该 8 次超额作任何自行豁免，
是否追认属用户／Codex 决定；执行者只如实登记消耗、失败与保留的日志。

#### split 缺口与 r5 修复

r4 的 `resolve_fold` 只做五项检查：task 相符、fold 存在、`sample_count == cohort_rows`、
三集合两两不交、以及**集合大小之和**等于 `sample_count`。因为最后一项按集合大小求和而非按数组长度，
它无法排除下列情形，这些都是本轮补上的缺口：

- 未校验 `protocol == outer5_inner20` 与 `validation_is_test == false`，
  一份"验证集即测试集"的 manifest 也能通过；
- **数组内部重复**：`train_indices = [0,0,1,2]` 时集合大小为 3，三集合并集仍覆盖全部行，
  旧检查通过，但训练会在第 0 行上训练两次；
- **负索引**：`train_indices = [-1,1,2]` 的集合大小同样是 3，旧检查通过，`-1` 会被当作合法行号；
- **非整数**：`[0,1,'2']` 之类的条目在旧实现里被 `int(v)` 静默接受。

修复后的契约（`split_indices()` + `resolve_fold()`，仍只读、不重建 manifest）：

1. `protocol` 必须严格等于 `outer5_inner20`；
2. `validation_is_test` 必须显式为 `false`（严格布尔，缺失或其它值都拒绝）；
3. 三个索引数组的每一项必须是整数（显式排除 `bool`），且 `0 <= index < sample_count`；
4. 每个数组内部不得重复；
5. `train/validation/test` 两两不交；
6. 三集合并集严格等于 `set(range(sample_count))`。

#### 新增 CPU 合成测试（全部走生产 `resolve_fold`，无另写校验器）

`tests/test_glt_3d_gain_d2.py` 新增 5 项：

| 测试 | 覆盖 |
| --- | --- |
| `test_resolve_fold_accepts_a_complete_split` | 合规 manifest 通过，并检查 evidence 中的协议／标志／覆盖字段 |
| `test_resolve_fold_rejects_a_manifest_that_is_not_the_fixed_protocol` | 错误协议、以及缺失 protocol |
| `test_resolve_fold_rejects_a_shared_validation_test_flag` | `validation_is_test=True` 与缺失该标志 |
| `test_resolve_fold_rejects_broken_index_arrays` | 重复索引（含并集仍完整、旧检查通过的那一种）、负索引、越界索引、非整数索引、数组不可用 |
| `test_resolve_fold_rejects_missing_and_overlapping_rows` | 缺失行（并集不覆盖）、跨集合重叠、cohort 行数不符 |

回归：`python3 -m pytest tests/test_glt_3d_gain_d2.py tests/test_glt_3d_gain_r2.py -q` → **41 passed**
（D2 文件 30、r2 文件 11），无新增告警。

#### 六个真实开发单元的只读校验

命令与输出：`logs/glt_3d_gain_20260921/r5_split_check.log`（`PYTHON_EXIT=0`）。
数据来源为 cohort manifest 的 `task_counts` 与 `split_manifest_sha256`，**未读取队列记录、
未访问 outer-test 标签或预测、未加载任何模型**。

| 单元 | train | validation | test | sample_count | protocol | validation_is_test | split 文件 sha256 与 cohort 绑定一致 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| xc/fold0 | 276 | 69 | 87 | 432 | outer5_inner20 | false | 是 |
| xc/fold1 | 276 | 69 | 87 | 432 | outer5_inner20 | false | 是 |
| eps/fold0 | 244 | 61 | 77 | 382 | outer5_inner20 | false | 是 |
| eps/fold1 | 244 | 61 | 77 | 382 | outer5_inner20 | false | 是 |
| eat/fold0 | 249 | 63 | 78 | 390 | outer5_inner20 | false | 是 |
| eat/fold1 | 249 | 63 | 78 | 390 | outer5_inner20 | false | 是 |

六个单元在新契约下**全部接受**（`UNITS 6 ALL_ACCEPTED True outer_test_reads 0`），
且磁盘上的 split 文件与 cohort manifest 记录的哈希逐一吻合 —— 说明收紧后的校验没有改动 split 本身，
只是把此前未检查的部分补上。

#### 修改文件

| 文件 | 变化 |
| --- | --- |
| `scripts/finetune_glt_3d_gain_d2.py` | 新增 `SPLIT_PROTOCOL`／`SPLIT_NAMES` 与 `split_indices()`；`resolve_fold()` 增加协议、`validation_is_test`、索引整数性/范围/去重与并集覆盖检查，证据字段扩展 |
| `tests/test_glt_3d_gain_d2.py` | 新增 5 项 split 契约合成测试 |

`src/`、`configs/`、数据缓存、split manifest 与既有产物均未修改。

#### 结论与限制

- 本轮是**实现与测试层级**的收口，不产生任何预测性能结论：D2 的候选判定仍缺 24 units 的 macro3 与
  XC 两折证据，**本轮不构成开发阶段授权**。
- **r3 的旧 smoke 不能作为修复后的公平对照结果**：其四臂既未共享公共随机流（r4 修复），
  split 契约也未收紧（r5 修复），其数值与产物只作历史记录保留。
- 本轮未做任何 GPU 验证（按授权），也未扩大任何预算。

### r6 执行记录：D2 四臂开发比较（24 units）

**状态：D2 开发比较完成，待 Codex 审查。** 执行者 ZCode，2026-09-21 UTC。
产物根目录 `results/glt_3d_gain_20260921/d2_development/`，日志根目录
`logs/glt_3d_gain_20260921/d2_development/`，网格日志
`logs/glt_3d_gain_20260921/d2_development_grid.log`。
**24/24 单元 PASS、0 失败、0 重试**；未启动预训练、D3、PH、新 probe 或额外 seed；
未访问 outer-test、未做 OOF 或 refit；未追加独立 smoke。

#### 执行前核对

`git status` 干净，`HEAD = 751a35b`（r5 提交），`git pull --ff-only origin dev` 为 no-op；
`tmux Uni-Poly` 无活动训练进程；`nvidia-smi --query-compute-apps` 未列出任何计算进程，
四张 GPU 显存占用为 15／15／15／492 MiB（GPU3 的常驻占用在 r5 之前即存在，非本计划启动）；
启动前核对现有产物，`d2_development/` 目录尚不存在，未重复启动。

#### 冻结的实验合同（实际执行值）

| 项 | 值 |
| --- | --- |
| 部署包 | `results/glt_pred_20260918/s3b_formal/b_fp/pretrain/deploy_05000.pt`（S5 `B_FP` step5000） |
| 配置 | `configs/mts/glt_pred_s3b_b_fp.json`（seed 42、patience 10、batch 32、eval_batch 64、wd 0.02） |
| 四臂学习率 | `f2d` o8 1e-5／norm 1e-4／head 1e-4；`fbase` +glt 1e-5；`fnorm` norm→1e-5；`fstable` +glt→3e-6 |
| 生产路径 | r4/r5 修复后的 runner：公共随机流隔离、`DualArm` 私有 GLT 流、严格 split 契约 |
| schedule | 总长 30 epochs、warmup 5 epochs，早停只改变实际停止位置（patience=10） |
| split | `mips_outer5_inner20`，train-only scaler，validation 选最佳 checkpoint |
| 初始化 | 每单元从同一部署包＋同一公共初始状态开始，未从旧 smoke 或其它 task/fold 继续 |

#### 启动与调度

- 新增 `scripts/run_glt_3d_gain_d2_development.py`（沿用项目既有“有界 GPU slot 子进程网格”方式，
  未新增并行框架）：每 slot 一次只跑一个单元，`CUDA_VISIBLE_DEVICES` 绑定单卡。
- 启动前用 `--dry-run` 固定 24 项清单并写入 `d2_development/schedule.json`（已存在的 schedule 或
  已存在的单元目录会直接拒绝启动）。
- 命令：`python3 scripts/run_glt_3d_gain_d2_development.py --gpus 0 1 2 3
  --output results/glt_3d_gain_20260921/d2_development
  --log-root logs/glt_3d_gain_20260921/d2_development`，tmux window `glt3d_r6_dev`。
- 每单元独立保存 `run.json`／`runtime.json`／`metrics.json`／`best.pt` 与独立日志
  `<arm>_<task>_fold<k>.log`。网格墙钟 **833 s**，`grid_runtime.json` 记录逐单元退出码与耗时。
- **`best.pt` 只是 validation 最佳权重，不是完整的训练 resume 状态**：它不含 optimizer、
  scheduler、epoch 计数与数据顺序，**不冒充可精确恢复**；本轮也没有任何单元需要恢复
  （0 失败），因此未使用、也未测试 resume 路径。

#### 预算消耗

| 项 | 授权 | 实际 |
| --- | --- | --- |
| development units | 24 | **24**（全部 PASS） |
| 每单元 epochs | ≤30 | 20–30 |
| 总 epochs | ≤720 | **668** |
| optimizer updates | —（由 epochs 决定） | **5544** |
| 失败／重试 | 不自动重训 | **0 失败、0 重试** |
| outer-test／OOF／refit | 不允许 | 未发生 |

#### 逐单元结果（validation R²，最佳 checkpoint）

| arm | xc/f0 | xc/f1 | eps/f0 | eps/f1 | eat/f0 | eat/f1 |
| --- | --- | --- | --- | --- | --- | --- |
| f2d | 0.41430 | 0.41863 | 0.79087 | 0.82078 | 0.98088 | 0.98126 |
| fbase | 0.34779 | 0.41524 | 0.79121 | 0.85125 | 0.98683 | 0.98719 |
| fnorm | 0.34756 | 0.41521 | 0.79119 | 0.85125 | 0.98684 | 0.98818 |
| fstable | 0.35281 | 0.42699 | 0.79384 | 0.85127 | 0.98513 | 0.98814 |

- 实际停止位置（executed epochs）：`eat` 八个单元全部跑满 30；`eps` 为 25–30；
  `xc` 为 20–30（其中 4 个在 21–22 提前停止，其余 28–30）。
- **触及 30 上限的单元：`fstable/xc/fold1`（best_epoch=30）**；按预注册要求**不因此延长训练**。

#### 逐任务两折均值与配对差值

| arm | xc | eps | eat | macro3 |
| --- | --- | --- | --- | --- |
| f2d | 0.41646 | 0.80583 | 0.98107 | **0.73445** |
| fbase | 0.38152 | 0.82123 | 0.98701 | 0.72992 |
| fnorm | 0.38139 | 0.82122 | 0.98751 | 0.73004 |
| fstable | 0.38990 | 0.82256 | 0.98664 | 0.73303 |

| 配对差 | 任务两折均值 | fold0 | fold1 | macro3 |
| --- | --- | --- | --- | --- |
| **FBASE − F2D** | xc **−0.03495**；eps +0.01541；eat +0.00594 | xc −0.06650；eps +0.00034；eat +0.00595 | xc −0.00339；eps +0.03047；eat +0.00594 | **−0.00453** |
| **FNORM − FBASE** | xc −0.00013；eps −0.00001；eat +0.00050 | xc −0.00023；eps −0.00002；eat +0.00001 | xc −0.00003；eps 0.00000；eat +0.00099 | **+0.00012** |
| **FSTABLE − FNORM** | xc **+0.00852**；eps +0.00134；eat −0.00087 | xc +0.00525；eps +0.00265；eat −0.00171 | xc +0.01179；eps +0.00002；eat −0.00004 | **+0.00299** |

#### 候选对照与门槛（沿用原门槛，未事后降低）

| 候选 | 参照 | macro3 | xc 均值差 | xc fold0／fold1 | 任务不降超 0.01 | 通过 |
| --- | --- | --- | --- | --- | --- | --- |
| fnorm | fbase | +0.00012 | −0.00013 | −0.00023／−0.00003 | 是 | **否**（macro3、xc 均不足） |
| fnorm | f2d | −0.00441 | −0.03508 | −0.06673／−0.00343 | 是 | **否** |
| fstable | fbase | +0.00311 | +0.00838 | +0.00502／+0.01175 | 是 | **否**（macro3 与 xc 均值均低于门槛） |
| fstable | f2d | −0.00142 | −0.02656 | −0.06148／+0.00836 | 是 | **否** |

**两个候选均未通过，聚合器 selection 为空**（门槛要求：相对 FBASE **和** F2D 的 macro3 均 ≥ +0.005，
且 XC 均值 ≥ +0.01、XC 两折均为正；任一任务两折均值不降超 0.01）。因此不进入“按 macro3 增量、
再按 XC 增量、再取较简单 FNORM”的选择步骤；**两个候选的完整结果均已列出，未隐藏任何一方**。

#### 关键观察（限定于本轮开发比较）

1. **并入 3D 路线在本轮没有带来整体增益**：`FBASE − F2D` 的 macro3 为 `−0.00453`，
   且 XC 两折均为负（−0.06650／−0.00339）。收益集中在 eps（+0.01541，几乎全部来自 fold1）
   与 eat（+0.00594，两折一致）。
2. **降低 norm 学习率（FNORM）在本轮等价于无效改动**：与 FBASE 的差异落在第 4–5 位小数
   （macro3 +0.00012，逐折差最大 0.00099），说明当前任务分组不是瓶颈所在。
3. **唯一有动向的杠杆是把 GLT 学习率再降一个量级（FSTABLE→3e-6）**：相对 FNORM 的 XC +0.00852
   且两折均为正，macro3 +0.00299；但既未达到 macro3 ≥ 0.005，也未达到 XC ≥ 0.01，
   且 eat 出现 −0.00087 的小幅回退。相对 F2D 的 XC 仍为 −0.02656。
4. **XC 的困难集中在单折**：`fbase/xc/fold0` 0.34779 明显低于 `f2d/xc/fold0` 0.41430，
   而 fold1 两者接近（0.41524 vs 0.41863）。本轮是单 seed、两折开发比较，
   不能据此推断总体或其他 seed 的行为。
5. 公共随机流末态摘要因各臂早停长度不同而不同，**不作跨臂相等要求**（已按 r6 说明处理）。

#### 聚合与验收方式

- `scripts/aggregate_glt_3d_gain_d2_development.py` 只接受 **24 个预定身份**，逐个核验：
  `runtime=PASS`、stage=development、`outer_test=NOT_RUN`、split 与固定折一致、
  记录的 config 等于冻结配置、命令中的部署包等于预期包、学习率表等于该 arm 合同、
  `requested_epochs ≤ 30`、`executed_epochs == history 长度`、
  `optimizer_updates == 逐 epoch steps 之和`、`best_epoch`／`best_validation_r2` 与 history 一致、
  必需产物存在、指标有限、f2d 无私有 3D 流而双路臂报告该流。
  **目录存在或日志末尾的 PASS 都不作为完成证据。**
- 结果：`{"status": "COMPLETE", "accepted": 24, "problems": 0}`，退出码 0；
  产物 `d2_development/aggregate.json`、`d2_development/report.md`，
  日志 `logs/glt_3d_gain_20260921/d2_development_aggregate.log`。
- 聚合器测试使用合成 JSON（24 个伪造单元），覆盖接受路径与 14 类拒绝条件
  （状态、stage、outer_test、requested/executed epochs、updates、best 值、
  非有限指标、config、学习率表、部署来源、split、身份、缺失产物、随机流契约），
  以及门槛与选择顺序；`tests/test_glt_3d_gain_d2_aggregate.py` **6 passed**，不启动模型任务。

#### 未执行项与限制

- 未追加独立 smoke、未做 lr sweep、未延长训练（含触及 30 上限的单元）。
- 未做预训练、D3、PH、新 probe、额外 seed；未访问 outer-test；未做 OOF 或 refit。
- 本结果属于**单 seed、三任务两折的 development 比较**，不是独立盲测，
  也不是正式八任务结果；折间差异与早停位置都会影响小数位级别的差值，
  因此 `+0.00311` 这类小增量不能读作稳定收益。

#### 结论

按预注册门槛，**FNORM 与 FSTABLE 均未晋级**；同时 `FBASE − F2D` 为负，
说明在当前单冻结 Trimer 协议与 30-epoch 开发预算下，**没有建立可归因于 3D 路线的整体预测增益**
（任务间收益抵消，XC 明显受损）。按计划 §7 与 r6 第五节，**无收益允许 STOP**：
不追加 sweep、不启动 D3、不增加 seed。是否接受该 STOP、或另立更丰富几何来源的新计划，
交 Codex 裁定；**执行者不自动晋级**。

### Codex 审查与下一步（执行后填写）

r1–r5 的审查意见已分别由 r2–r6 逐条回应；历史数值与产物保留未改，必要更正以带标记的追加形式记录。
待 Codex 审查 r4／r5 的修复、r4 的 8 次 GPU 调用超额，以及本轮 r6 的 24-unit 开发比较结果后，决定：
(a) 对 r4 超额的结论（追认或返修）、(b) 接受 D2 的 STOP 结论或要求返修、
(c) 是否另立新计划（例如审计 XC 标签／条件覆盖，或更换几何来源）。
**技术自检通过不替代这一审查，执行者不自动晋级。**
