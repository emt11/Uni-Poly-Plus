# GLT 3D 优化：验证非键空间消息的预测增量

计划 ID：`GLT-SPATIAL-20260921-01` ｜ 修订：r1 ｜ 日期：2026-09-21 UTC

状态：**待授权执行**。本轮用户授权 Codex 制定并覆盖本文件；不等于授权实现、GPU 验证或训练。

角色：Codex 规划和审查；ZCode 在用户授权后执行。基准 commit：`3912249`，分支 `dev`。制定时存在用户的未跟踪 `.zcodeignore`，不修改、不提交该文件。

本文件是用户明确指定的本次 3D 计划入口；开始执行前同时读取 `AGENTS.md`、`Plan.md`，若存在并行有效授权则核对冲突，不将旧计划视为本轮授权。**推荐首先仅授权 P0＋P1；P2/P3 预算列出用于审议，阶段间不得自动晋级。** PH 保持搁置。

## 0. 已知结果与本轮科学问题

旧 `GLT-3D-GAIN-20260921-01 / r6` 已按限定负结果关闭。完整旧文件可由 `git show 3912249:3D.md` 取得，实质性周期记录已存入 `PROJECT_HISTORY.md`，不重复归档或覆盖历史产物。

| 旧 D2 比较 | macro3 R² 差值 | XC 两折均值差值 | 边界 |
| --- | ---: | ---: | --- |
| FBASE − F2D | −0.00453 | −0.03495 | 当前冻结 Trimer、30-epoch 开发比较未建立整体增益 |
| FNORM − FBASE | +0.00012 | −0.00013 | 未达候选门槛 |
| FSTABLE − FBASE | +0.00311 | +0.00838 | 未达候选门槛 |

这些结果不证明所有 3D 信息无效。旧 r4 超额 8 次 GPU 调用仍为未获追认；新计划不抵销历史消耗。

**本轮只检验一个新假设：当前局限是否部分来自消息仅沿化学连接传播，未显式利用 Trimer 内空间接近的非键原子关系？**

目标是模型属性预测增量，不是更低重建 loss、更多非零梯度或更大的几何响应。先不换几何来源、读出方式、预训练任务和融合器，避免无法判断收益来源。仍使用有限开放 Trimer；空间接近不等于真实链间相互作用，更不等于恢复体相堆积。

## 1. 论文依据：已采用机制与项目改造严格分开

以下原文于 2026-09-21 核对。论文支持研究假设，不保证本项目收益。采用明确版本；不把预印本标成已核实的期刊发表。

### 1.1 MMPolymer，CIKM 2024

[MMPolymer: A Multimodal Multitask Pretraining Framework for Polymer Property Prediction，arXiv v2](https://arxiv.org/html/2406.04727v2)，重点 §3.2.2、公式 5–10、§3.3。

原文使用 Star Substitution 形成有限 RU 代理，以全原子对欧氏距离初始化 Gaussian pair 表征，更新 atom/pair 状态，以虚拟原子读出；预训练包括遮蔽、坐标去噪和跨模态对齐。

**支持什么**：聚合物有限几何代理中的原子对距离值得建模，3D 编码不必局限于化学键图。**不支持什么**：本文的 Trimer bond adapter、4 Å 截断、插入层和新损失；也不能从论文推导“必须生成长链”。本轮不复现完整 MMPolymer，不自动恢复已做过的 ALIGN。

### 1.2 Mol-TDL，2024 预印本

[Molecular topological deep learning for polymer property prediction，arXiv:2410.04765v1](https://arxiv.org/html/2410.04765v1)，重点 §3.1–3.2、Figure 1、§4 的消融。

原文以距离阈值构建 Vietoris–Rips complexes，阈值为 2、2.5、3、3.5、4 Å，处理原子、边、三角形及其邻接，并进行多尺度对比预训练。

**支持什么**：超出共价连接的空间邻接是聚合物预测中已有研究的方向，4 Å 位于论文研究范围。**不支持什么**：单独采用 4 Å 一定最优、仅加入边等价于完整拓扑模型，或其数据划分成绩可与本项目直接比较。本轮仅借鉴距离邻接，不实现 PH、多尺度、高阶 simplex 或论文整套训练。

### 1.3 可追溯的设计决策

| 决策 | 依据层级 | 本轮处理 |
| --- | --- | --- |
| 显式原子空间邻接 | 上述两篇论文的方法机制 | 作为唯一主要输入变化 |
| 保留单冻结 Trimer | 现有数据合同；有限代理有论文先例 | 不是“物理充分”的保证 |
| 4 Å 非键范围 | Mol-TDL 实验范围中的一个点 | 工程起点，不做半径 sweep |
| 距离 Gaussian 编码 | MMPolymer 的机制方向 | 32 通道为项目预算选择，非原文参数 |
| bond→atom→bond adapter | 为保留当前 GLT 的项目改造 | **新假设，非论文现成模块** |
| 第 3/5 层、128 宽、固定 0.1 残差 | 项目限定预算与梯度路径的工程选择 | **无论文最优性依据**，不包装为论文结论 |
| 保留既有三个预训练任务 | 控制变量 | 不宣称原文采用本项目损失 |

不存在已核实论文直接验证“本项目 GLT＋下述 adapter”的收益。本计划是**论文启发的有界验证**，不是文献复现或提前成立的性能优化。

## 2. 固定来源、输入与禁止事项

执行端先逐项复核当前配置、代码和实际路径；不凭旧模型名称选择版本。发现不一致先报告，不静默换 baseline。

| 项目 | 本轮指定来源 |
| --- | --- |
| 基础预训练合同 | `configs/mts/glt_pred_s3b_b_fp.json` |
| 共享初始状态 | `results/glt_pred_20260918/s3b_prep/common_init_v1.pt` |
| 预训练 split | `results/glt_pred_20260918/s3b_prep/pretrain_split_v1.json`，使用其中 train |
| 历史部署参考，不作为新轨迹起点 | `results/glt_pred_20260918/s3b_formal/b_fp/pretrain/deploy_05000.pt` |
| 下游 cohort | `data/processed/glt_dual_v2/downstream/cohort_1545eda5a8f6a1` |
| 下游基础几何 | `data/processed/mips_trimer_scage_downstream` |
| 下游 static | `data/processed/glt_dual_v2/downstream/dual_static_v1` |
| 下游 split | `data/splits/mips_outer5_inner20` |

预训练缓存路径从上述既有配置/运行记录和 cohort 解析，不能自行选另一个同名缓存。核对现有身份绑定即可，不新增通用 hash/schema 框架。

保持：O8 的 2D 输入、层数和数学结构；GLT 六层 512 维主干及原化学 line-path/角度定义；中心内部键 readout；concat 融合与属性 head；不使用 MD200。旧 D2 的学习率返修与 RNG 隔离机制可复用，但不选其最佳历史 checkpoint 当本轮 matched reference。

禁止：构象生成、坐标优化、多构象、RU±2/无限周期坐标拼接、缓存重建/清理、PH/CLS/TORSION 新任务、FGR/ALIGN 重启、FP 替换、额外 seed、outer-test、OOF、train＋validation refit。

## 3. 新输入合同：真实物理原子，不合并 image

### 3.1 新增最小字段

```text
spatial_pos          float [A, 3]
spatial_atom_batch   int64 [A]
physical_bond_ends   int64 [B, 2]
physical_bond_batch  int64 [B]
chemical_edges      int64 [2, E_chem]  # 双向，无 exact self、无重复有向边
```

`B` 必须与现有 GLT physical bond token 行数及顺序完全一致。`A` 是这些物理键端点覆盖的真实重原子集合，不加入氢、dummy 或端帽猜测；孤立原子不凭空创建键 token。输入依赖已有显式映射，不能用 RDKit atom order 猜身份。collate 分别偏移 atom 与 bond 索引。

核对端点元素、真实键类型、端点距离与原 bond length、中心键标志及三 RU 身份。不同 RU 的同名原子保持不同物理记录；不平均左右距离、不按 canonical ID 去重、不强制左右位置对称。

invalid 几何在进入模块前跳过，不以 NaN×0 处理。中心内部键为 0 时中心 readout 仍为空：不得用跨键代替中心监督或分母，学生原子任务照常参加；空间模块即使存在侧键，也不改变该定义。

### 3.2 两种关系集合

`CHEM`：使用全部真实共价键的双向原子关系。

`SPACE`：使用 CHEM 与同图内所有满足 `0 < d(a,b) < 4 Å` 的非键原子对之并集。两方向都保留。共价键即使距离超过 4 Å 也保留；物理 atom exact self 排除。不同物理原子坐标重合为数据问题，记录并停止受影响样本审计，不当作 self 删除。

不使用 TopK、不按序号裁边，不跨 packed graph，不重复加入已存在的共价关系。可以精确分块计算距离以降低临时显存，但不能改变边集合。

关系权重固定为：共价边 `w=1`；非键边 `w=(1+cos(pi*d/4))/2`。这是使截断边贡献连续趋零的工程设计，不是上述论文原公式，也不是物理相互作用能。

### 3.3 噪声与监督隔离

预训练 `spatial_pos` 必须来自**当前 forward 已经生成的那份 noisy 坐标**，与原距离/角度输入共享一次坐标扰动；空间距离、邻域成员和权重全部由它计算。不得从 clean 坐标固定邻域，再只替换距离；不得额外消耗噪声 RNG。

clean 坐标仍只在既有目标构造路径消费。微调使用冻结 clean 坐标。参考接线位置为 `src/dataset/glt_dual_pretrain.py` 中 noisy 坐标物化路径，以及 `src/dataset/glt_dual.py` 的 clean 几何物化路径；执行端须核对真实接口后实现。

## 4. 模型：最小空间消息 adapter

原六层 GLT 正常执行。在**第 3、5 层 block 完成后**分别插入一个 adapter；两个 adapter 不共享参数。它们只修改物理键 states，之后继续原层与原中心 readout，不新增独立持久 atom states。

令当前键状态为 `H [B,512]`。每个物理原子先聚合入射键：

$$
u_a=\operatorname{LN}_{128}\left(W_{in}\,\operatorname{mean}_{e\ni a}H_e\right).
$$

无入射键时显式取零并跳过该原子；不得空均值。距离编码为 32 个固定 Gaussian，中心 `c_k=4k/31 Å`，宽度 `s=4/31 Å`，`RBF_k(d)=exp(-0.5*((d-c_k)/s)^2)`；共价长边的真实距离不 clamp。

$$
q_{a\leftarrow b}=\operatorname{MLP}_{289\to256\to128}
([u_a,u_b,\operatorname{RBF}_{32}(d_{ab}),\mathbf{1}_{bonded}]),
\qquad
m_a=\frac{\sum_b w_{ab}q_{a\leftarrow b}}{\max(1,\sum_b w_{ab})}.
$$

$$
H'_{(a,b)}=H_{(a,b)}+0.1\,W_{out}(m_a+m_b),\qquad W_{out}:128\to512.
$$

中间激活 GELU；adapter 不使用 dropout。Linear 使用 Xavier uniform，bias 置零，`W_out` 无 bias；LN weight=1、bias=0。`W_out` 不零初始化，系数 0.1 固定、不可学习，不引入会关闭整条新分支的零 gate。固定残差是本轮控制条件，不保证优于学习 gate。

聚合在 FP32 累加，回写前转为 H.dtype，记录混合精度行为。端点以和聚合，避免键端点顺序引入方向标签。仅标量距离意味着旋转、平移、反射不变；**不具备手性分辨、等变向量或无限链保证**。

`CHEM` 与 `SPACE` 使用上述完全相同参数和算子，只改变关系集合及随关系定义的权重。空间边数量与传播范围也随之变化，因此差值是“增加非键距离消息”的整体效应，不能称为纯粹距离数值效应。

## 5. 对照与可归因范围

### 5.1 三条匹配预训练轨迹

| arm | GLT | adapter | 用途 |
| --- | --- | --- | --- |
| BASE | 原 GLT | 无 | 新运行的旧架构 reference |
| CHEM | 原 GLT | 仅共价邻接 | 新增参数/消息模块控制 |
| SPACE | 原 GLT | 共价＋非键空间邻接 | 唯一主候选 |

三条轨迹从同一个原始 common-init 开始，各训练 5k；**不能将 SPACE 从旧 BASE 5k 热启动，而 BASE 不再训练**。所有共享 encoder、pretrain decoder、FP head 参数按名称复制；若既有 common-init 未包含某个共享 head，按原初始化规则在独立 seed42 RNG 中构建一次并复制给三臂，保存该初态，不允许各臂自行构造而移动公共流。新 adapter 在独立 seed43 RNG 中初始化并在 CHEM/SPACE 间逐张量一致，不移动公共随机流；该 seed 仅用于模块初始化，不是新增实验重复 seed。

原 masked-atom CE、中心几何重建、7RU FP BCE 保持，权重 `[1,1,0.1]`；沿用各自实际 decoder/loss，不换为论文的坐标头。保留 FP 是控制变量，不把这轮变成再次筛选 FP。

### 5.2 四条微调路径

| arm | 起点 | 属性预测 |
| --- | --- | --- |
| O8_ONLY | 本轮 BASE step5000 中的 O8 | O8＋几何半边结构零 |
| BASE | 本轮 BASE step5000 | 原双路 concat |
| CHEM | 本轮 CHEM step5000 | 同结构化学消息模型 |
| SPACE | 本轮 SPACE step5000 | 空间消息模型 |

O8_ONLY 不实例化/执行 GLT，`norm2` 仅执行一次；使用与双路同形状 1024 输入 head，几何半边为零，披露无效权重数量。它是**双路预训练包抽取的 2D-only**，不是独立单模态预训练模型。BASE/O8_ONLY 共享初始化 head；四组 head 按同一 initial artifact 复制。

三个关键比较同时报告：SPACE−CHEM、SPACE−BASE、SPACE−O8_ONLY；另报 CHEM−BASE。历史 D2 与本轮不是可直接合并的 matched runs。

## 6. 固定训练协议

### 6.1 预训练

```yaml
seed: 42
world_size: 4
microbatch_per_rank: 84
gradient_accumulation: 3
global_batch: 1008
optimizer: AdamW
learning_rate: 0.0002
weight_decay: 0
precision: bf16
max_updates: 5000
warmup_steps: 2000
schedule_total_steps: 20000
end_lr: 0.000000001
atom_mask_ratio: 0.30
noise_sigma: 0.03
loss_weights: [1, 1, 0.1]
save_every: 1000
```

保留既有 scheduler 公式与数据顺序，5k 是 20k schedule 的前段，不缩短 schedule 到 5k。未列项继承上述明确配置并保存 resolved config。初始数据缺失、GPU 数量或有效 batch 不兼容时报告，不自行改预算。

固定 step5000 导出，不根据本轮下游 validation 选择预训练 checkpoint。已有预训练验证若运行，仅按原频率作诊断；不额外新增模型前向。保留既有来源/重叠证据标签，不称独立盲测。

在既有训练步骤统计各 loss、adapter 梯度范数/更新量、残差比例、关系数、有效几何数、速度/显存；step0 只记录初始参数，后续记录 step1/2/1000/2000/3000/4000/5000。不为监测另做 backward 或重放数据。

### 6.2 下游

四臂 × `xc/eps/eat` × `fold0/1`，共 24 units，seed42。协议 `outer5_inner20`，同一固定 manifest。生产 `resolve_fold` 必须保留 r5 的协议、严格布尔标志、整数/范围/去重/互斥/全覆盖检查。

FULL adaptation，O8/GLT（包括 adapter）lr=1e-5，norm/head lr=1e-4；AdamW、weight_decay=0.02、MSE，train batch32、eval batch64，最多30epochs，warmup5epochs，patience10。保持 D2 的 scheduler 数学与参数分组，**不是 FNORM 或 FSTABLE 配置**。

标签 scaler 仅拟合最终 train；仅 validation R² 选最佳 epoch。每次 validation 保存当前预测对应的 sample keys；最终保存被选 epoch 的预测，无需为报告额外跑整轮。outer-test 只允许读取 split 索引做完整性校验，不读取其标签、编码、预测；不 refit。

复用已修好的 GLT 私有 RNG 流和公共 O8/head 流：前者跨 batch 推进、异常恢复，后者不被 GLT/adapter 干扰。记录运行期共享初始张量与样本顺序，不能以事后重建初值替代运行期证据。

部署记录 adapter 模式/结构，strict-load；错误模式拒绝加载。`best.pt` 不冒充完整 resume。若需恢复，必须已有被验证的全状态恢复路径，否则停止申请后续安排，不从 best.pt 静默接着训练。

## 7. 阶段、预算与门槛

**下列各阶段都须得到用户授权才可运行。推荐第一次仅授权 P0＋P1。失败和重跑的实际消耗均计入额度。**

### P0：来源与非键关系可用性审计

模型调用=0、Ridge fits=0、optimizer updates=0、构象生成=0，CPU 最多30分钟。读取现有记录，不对全量缓存做新内容扫描。

1. 核对上述缓存/初始化/split、代码基线、工作区、活动进程；独立 tmux window，不干扰已有训练。
2. 预训练按 sample key 字节排序选前512个 train 样本；另检查六个开发单元的 train/validation 中去重结构。只做现有结构物化和关系统计，不读取 outer-test 结构。
3. 输出映射一致性、CHEM/SPACE 边数分布、新增非键比例、跨 RU 关系比例、最大图规模、N=0 与 invalid 数量；不得用目标 R² 选择半径。
4. 在现有数据说明中确认 XC 的目标名称、单位、标签来源与是否带实验条件；找不到写“未核实”，不凭缩写断言是结晶度，也不伪造温度/链长字段。此项缺证不自动否定空间关系实验，但限制物理解读。
5. 任一真实映射无法证明，先阻断。若所有被检查有效样本 SPACE 与 CHEM 完全相同，本方案没有被检验变量，停止，不直接扩大半径。

### P1：实现、局部验证与有界 smoke

先做无模型的报告/退出码故障测试，再运行昂贵验证。拟新增一个空间 adapter 文件；在真实 dataset/collate/GLT 接口中以默认关闭的开关接线，原 BASE 路径不得改变。不开发通用实验框架。

必要覆盖：

1. 真实端点映射、batch 不串图、同名物理副本不合并、化学边只计一次/方向、边界4Å及零边处理。
2. N=0、invalid、空中心 readout 无 NaN，中心损失分母正确，原子/FP 任务仍可训练。优先沿用现有真实 N=0 key；不存在可用样本时明确使用确定性 fixture，不静默 skip。
3. 置换、平移/旋转/反射不变性；用合成四原子链扭转改变非键距离，检查 SPACE 的关系/输入有响应，CHEM 关系不变。不要求完整 GLT 对扭转完全无响应。
4. 新邻域与 RBF 均由同一 noisy 坐标计算；sigma0 与 clean 对齐、非零噪声改变输入；无 clean 旁路、无额外噪声抽样。
5. CHEM/SPACE 新参数一致、公共状态与 RNG 一致，新增模块有有限有效梯度；未更新前不同关系允许输出不同。
6. DDP 部分 rank 无有效中心几何、全部 rank 无有效中心几何时，通信/backward 完成，CE/FP 有限；不能以乘零掩盖 NaN。
7. strict-load、O8_ONLY 不依赖几何、BASE 开关关闭 parity；先保存 checkpoint/核心指标，再写诊断，失败记录非 PASS。launcher 保存真实退出码，拒绝覆盖单元。

硬预算：

| 类型 | 上限 | 计数口径 |
| --- | ---: | --- |
| 局部 CPU 模型测试 | 64 forwards＋32 backwards，0 updates；15分钟 | 真实与 toy 模型调用均计入，其他纯索引/JSON测试单独记录 |
| 三条预训练 smoke | 每臂2 updates，总6 updates | 与正式 world/batch/schedule 一致，仅提前停止 |
| DDP 空几何额外验证 | partial/all 各1次分布式 forward/backward，0 updates | 共2次分布式调用，4 ranks即8次rank级调用 |
| 四条微调 smoke | XC/fold0，各1epoch，总4epochs | 对应预训练 smoke 导出包；30epoch schedule，仅提前停止 |

GPU、worker 或超过一分钟的运行必须在 `tmux Uni-Poly` 独立 window 留日志。P1 所有运行报实际 forward/backward/update/epoch 数；阶段预算各自独立，不以“只报告失败”抹去训练消耗。

报告代码故障优先从已保存历史恢复报告，不能自动完整重跑。耗尽任何类别额度即停止交回；不得挪用尚未授权 P2/P3 预算。

P1 验收：接口/科学边界/梯度/部署/RNG/失败语义通过，且计算可承受。SPACE 若每步时间或峰值显存超过 BASE 的2倍，标记成本待审查，不擅自裁边。smoke 不排名。**P1 交付后停止，Codex 审查后再申请 P2。**

### P2：三条匹配 5k 预训练（待另行授权）

最多3×5000=15000 optimizer updates，必须从共同原始初值重新开始，不继续 P1 smoke。三臂复用完全相同 cohort 和公共随机流。遇到持续非有限、身份问题、参数不更新或样本错配停止受影响阶段。

交付三份固定 step5000 部署包、严格加载证据、损失与模块梯度/残差曲线及成本。若只能证明 loss 下降，仍不宣布空间信息有用。完成后交回，**不自动启动 P3**。

### P3：24-unit 开发比较（待另行授权）

最多24×30=720epochs。失败/重跑均计入；无追加 seed、task/fold 或超长训练。全部单元成功且身份正确才汇总 COMPLETE；早停按实际 history 长度记数。

逐任务先平均两折，再平均三任务得到 macro3。报告完整24单元、逐折差值、最佳epoch/是否撞上限、成本，不能只给均值或挑最佳组。

SPACE 只有同时满足以下条件才列为后续确认候选：

- 相对 **BASE、CHEM、O8_ONLY 每个对照**，macro3 R² 增量均≥0.005；
- 相对上述每个对照，XC 两折均为正、XC 均值增量≥0.01；
- 相对上述每个对照，任何任务的两折均值退化不超过0.01；
- 无来源、泄漏、初始化、模式、预算或不完整产物风险。

这些是预登记工程门槛，不是论文阈值或显著性检验。validation 已反复参与开发；单seed两折不构成独立盲测。best_epoch 撞30单列风险，不自行延长。

解释规则：SPACE 只优于 BASE 但不优于 CHEM，不能将收益归为非键空间关系；只优于 CHEM 但不优于 O8_ONLY，不能宣布值得保留3D。CHEM 单独有收益可形成下一步建议，但不自动新开实验。

### P4：后续确认，不包含在本轮预算

P3 通过才由 Codex 另写独立确认计划，冻结选择后讨论更多 seed/任务与最终测试。未通过则以“当前空间 adapter 未建立增量”停止；不自动转向多尺度、PH、改半径、改 gate 或更换构象。

## 8. 实施范围、产物与退出规则

建议新增 `src/modules/glt_spatial_adapter.py`；复用并最小扩展现有 GLT/dataset 接口。runner 优先复用 `scripts/finetune_glt_3d_gain_d2.py` 已验证的 split/RNG/记录函数；新入口和配置以 `glt_spatial` 命名，不修改旧计划产物和历史运行配置。

不要直接复制 GALPH 模型作为新主干，不默认加载 PH/CLS 权重。实际文件清单由 P1 依据真实接口确定并报告，数学合同不能随实现方便改写。

```text
results/glt_spatial_20260921/
  p0/                         # 来源、映射、关系覆盖
  p1/                         # tests/smoke，独立失败目录
  p2/<base|chem|space>/        # 新的预训练轨迹
  p3/<arm>/<task>/fold<id>/   # 微调单元
logs/glt_spatial_20260921/
```

每单元保存 resolved config、运行代码/初始化/部署/split 身份、参数分组与数量、实际预算、history、best.pt、有限 validation 预测、metrics/runtime。继承必要身份检查，不另建重复 manifest 框架。aggregator 校验完整性、协议、arm/task/fold、step、模式和有限指标，拒绝 smoke/formal 混用与部分产物。

停止条件：无法证明映射；invalid 被当作有效；读取 clean 邻域绕过噪声；跨图/划分泄漏；共享随机流被额外模块消耗；checkpoint 不兼容；NaN/Inf；写入冻结缓存；覆盖产物；超预算；需要改变科学定义才能继续。停止后允许只读定位，未经授权不扩大运行。

## 9. 当前交付与执行记录

当前只有计划文档。论文已核对；具体 adapter 是明确标注的项目改造，**尚无该模型实现、测试或预测增量证据**。

| 阶段 | 当前状态 |
| --- | --- |
| P0 来源/关系审计 | 待授权，未执行 |
| P1 实现与 smoke | 待授权，未执行 |
| P2 三臂5k | 待后续授权，未执行 |
| P3 24 units | 待后续授权，未执行 |
| P4 确认 | 仅决策方向，无预算授权 |

ZCode 获授权后在此追加实际执行记录：基准、改动、命令/window、结果/失败、真实预算、证据路径、偏差、待审查项。不得改写计划门槛来使结果通过。Codex 审查后明确通过/返修/停止及下一步；无充分新证据时允许结束，不为延续研究堆叠模块。
