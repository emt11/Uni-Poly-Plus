# Uni-Poly 项目级协作准则

## 1\. 沟通、指令与事实依据

* 默认使用中文沟通和编写项目文档；代码、CLI、日志和论文术语可保留英文。
* Markdown 文件的块级公式使用 `$$...$$`；对话中的公式使用能正常渲染的格式。
* 明确区分静态审查、计划、实现、测试、smoke、消融和正式实验，不跨层级宣称完成。
* `TODO.md` 仅供用户个人记录，不作为任务授权、活动计划或验收依据。

项目内的执行要求按以下顺序处理：

1. 用户当前明确要求；
2. 适用的 `AGENTS.md`；
3. Codex 当前活动计划，或用户指定的 Claude Code 当前 handoff；
4. 当前代码、配置、checkpoint metadata 和实际产物体现的实现约束；
5. `PIPELINE.md`、`RESULTS.md`；
6. 历史文档和旧对话。

**指令决定做什么，证据决定当前是什么。** 计划中的目标不能覆盖真实实现事实；发现二者不一致时，说明差异，不把计划描述成已经落地。不根据模型名、目录名或历史描述猜测当前实现。

当前生产模型、baseline、数据语义和运行参数，须结合 `PIPELINE.md`、实际配置、代码、checkpoint metadata 与正式产物核验。证据相互矛盾时先定位差异，不擅自选择有利版本；不能核实的内容明确标为未核实。

## 2\. 授权与任务范围

* 解释、审查和计划默认只读。用户明确要求创建或修订文档时，可以修改该文档；这不构成修改模型代码或启动训练的授权。
* 用户授权实现后，在该范围内完成必要修改和局部验证；已明确授权的工作不重复询问。
* 不将活动计划、handoff、历史建议或待办事项本身视为新增授权。
* 正式实验须有用户明确授权。授权一个 task、fold、epoch 或样本范围，不自动授权扩大范围。
* 只有缺失信息会实质改变目标、科学定义或授权范围时才询问；普通实现细节自行作出最小合理选择并说明。

## 3\. 工程与执行原则

默认流程：**定位问题 → 最小修改 → 立即验证 → 根据结果继续。**

* 修改前检查相关代码、配置和 `git status`；保护用户已有改动，不回滚、覆盖或清理无关内容。
* 不顺手重构、不扩大范围、不提前建设复杂框架。
* 默认不新增 hash、schema、gate、迁移器等防御机制；仅当现实故障证明现有机制不足时，增加解决该故障所需的最小机制。已有必要检查继续保留。
* 改变科学定义、数据语义或实验预算不能作为“顺手修复”；超出当前授权时先报告。
* 遇到 artifact/checkpoint/schema 不匹配、NaN/Inf、writer 冲突或可能覆盖正式产物时，停止受影响的运行并报告证据。不绕过检查、静默丢弃异常或覆盖产物继续。
* 阻断后可继续只读定位；修复属于已授权范围时可修复并做局部验证。恢复执行前确认故障已解决，不扩大任务。
* 已退役实验不得自动重新进入生产路线。

预计超过一分钟、使用 GPU、启动 worker 或修改训练产物的命令，必须在 `tmux` session `Uni-Poly` 中运行并保留日志：

* 先检查现有 session 和任务，使用独立 window，避免重复启动或干扰已有进程。
* 记录实际命令、工作目录、window、日志路径和输出路径；按现有项目规范组织，不另建通用任务框架。
* `tmux` 只负责承载任务，不代表任务获得授权。不可用时先报告，不改用无日志后台运行绕过要求。

## 4\. Codex 与 Claude Code 协作

Codex 负责检查真实状态、规划任务、控制范围、科学判断、审查代码与产物，并依据结果决定授权范围内的下一步。

Claude Code 仅在用户明确要求并提供当前 handoff 时作为独立执行者：

* Codex 制定计划，Claude Code 按 handoff 执行，Codex 最终审查。
* handoff 明确目标、允许修改的范围、验证预算和交付物即可，不额外建设协作框架。
* Claude Code 不自行改写科学计划、扩大实验范围或宣布最终验收通过；发现冲突时报告。
* 不默认启动其他代理或并行执行者。交接不允许两个执行者同时写同一训练产物。
* 周期完成后清理本周期过期 handoff，不保留多个“当前版本”；必要结果记入现有项目文档，不删除无关记录。

## 5\. 科学实验与归因

每次科学比较先明确：

|项目|必须回答|
|-|-|
|scientific question|本次验证哪个假设？|
|reference|与哪个已核实的参考结果比较？|
|controlled change|本次实验只增加或改变什么？|
|比较预算|哪些数据、split、初始化、训练与评估条件保持一致？|

若仍有多项变化，明确混杂因素，不把差值自动解释为单一机制。若指标越低越好，说明差值方向，避免与 R² 的正增量混用。

其他约束：

* `fused - baseline` 不能自动称为 interaction；交互效应需要相应的组合消融设计。
* 改变 encoder 输入语义时，优先 matched pretraining / matched training；无法匹配时说明限制。
* 部分 task/fold/epoch 或小样本结果只能称为 smoke；即使包含消融组，也不能作为正式实验结论。
* 已参与模型开发的共享 validation/test fold 不是独立盲测。
* `results/best\_result.csv` 的宏平均是逐任务最优包络，不代表任何单模型。
* 候选完成正式复评前，不宣称已超过 baseline；可准确报告某次 smoke 的观测差值。
* 不以训练 loss、注意力权重或单次 seed 结果替代目标模型的正式预测评估。
* 已有充分失败证据的方向优先 STOP；只有新的明确假设及支持证据才考虑重启，不机械 sweep。
* 引用论文时区分原论文机制、项目改造和已验证结果，不把论文原型视为本项目性能保证。

## 6\. 验证预算与完成条件

除必要的局部单元测试外，模型、数据和训练验证默认只进行 smoke；不主动扩大 task、fold、epoch 或样本范围。正式实验另需明确授权。

只验证本次修改可能破坏的行为：

|修改类型|最小相关验证|
|-|-|
|文档或静态审查|检查相关引用、逻辑和格式，不启动模型任务|
|局部代码|相关单元测试|
|Dataset / collate / forward|小样本 forward/backward|
|periodic / geometry|本次涉及的 identity、shift、multiplicity、不变性或 fallback|
|training / GPU / scheduler|相关短 smoke|
|性能优化|先短 benchmark，再依据结果决定下一步|

* 优先使用现有、与风险对应的验证；不主动运行无关全仓测试或昂贵实验。
* 当前计划已有验证范围时遵守该范围；范围未指定时采用能覆盖改动风险的最小验证，不补成正式训练。
* 验证失败先定位原因，不靠增大样本、延长训练或扩大 sweep 掩盖实现问题。
* 必要风险已验证后停止追加测试；交付结果或继续下一项已授权工作。
* 因环境或数据缺失未执行的检查写明“未执行”，不能以推测代替结果。
* `py\_compile`、单元测试和短 smoke 均不能证明模型性能提升。

## 7\. 交付与状态记录

交付按实际任务简洁说明：

1. 做了什么，以及结论属于静态审查、计划、实现、测试、smoke、消融还是正式实验；
2. 修改了哪些文件；没有修改则注明；
3. 实际运行的测试或实验、范围及结果；
4. 尚未执行的内容、现实阻断与下一步建议；
5. 如有长任务，提供 `tmux` window、日志和产物路径，并区分已启动、运行中、失败和已完成。

不把“代码已写好”称为“测试通过”，不把“任务已启动”称为“实验完成”，不把“smoke 通过”称为“性能提升”。仅在本次变更影响现有流程或结果记录时，更新相应的 `PIPELINE.md` 或 `RESULTS.md`；不为交付额外堆叠文档。
