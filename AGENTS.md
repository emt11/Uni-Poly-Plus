# Uni-Poly 项目级协作准则

## 1\. 沟通、指令与事实依据

* 默认使用中文沟通和编写项目文档；代码、CLI、日志和论文术语可保留英文。
* Markdown 文件的块级公式使用 `$$...$$`；对话中的公式使用能正常渲染的格式。
* 明确区分静态审查、计划、实现、测试、smoke、消融和正式实验，不跨层级宣称完成。
* `TODO.md` 仅供用户个人记录，不作为任务授权、活动计划或验收依据。

项目内的执行要求按以下顺序处理：

1. 用户当前明确要求；
2. 适用的 `AGENTS.md`；
3. 根目录 `Plan.md` 中已获用户授权的当前计划及修订；
4. 当前代码、配置、checkpoint metadata 和实际产物体现的实现约束；
5. `PIPELINE.md`、`RESULTS.md`；
6. 历史文档和旧对话。

**指令决定做什么，证据决定当前是什么。** 计划中的目标不能覆盖真实实现事实；发现二者不一致时，说明差异，不把计划描述成已经落地。不根据模型名、目录名或历史描述猜测当前实现。

当前生产模型、baseline、数据语义和运行参数，须结合 `PIPELINE.md`、实际配置、代码、checkpoint metadata 与正式产物核验。证据相互矛盾时先定位差异，不擅自选择有利版本；不能核实的内容明确标为未核实。

## 2\. 授权与任务范围

* 解释、审查和计划默认只读。用户明确要求创建或修订文档时，可以修改该文档；这不构成修改模型代码或启动训练的授权。
* 用户授权实现后，在该范围内完成必要修改和局部验证；已明确授权的工作不重复询问。
* `Plan.md` 记录授权范围，不产生授权；历史计划、归档和待办事项也不构成新增授权。已有授权持续有效，不因交接或换对话重复询问。
* 正式实验须有用户明确授权。授权一个 task、fold、epoch 或样本范围，不自动授权扩大范围。
* 只有缺失信息会实质改变目标、科学定义或授权范围时才询问；普通实现细节自行作出最小合理选择并说明。

## 3\. 工程与执行原则

默认流程：**定位问题 → 最小修改 → 立即验证 → 根据结果继续。**

* 修改前检查相关代码、配置和 `git status`；保护用户已有改动，不回滚、覆盖或清理无关内容。
* 不顺手重构、不扩大范围、不提前建设复杂框架。
* 默认不新增 hash、schema、gate、迁移器等防御机制；仅当现实故障证明现有机制不足时，增加解决该故障所需的最小机制。已有必要检查继续保留。
* 改变科学定义、数据语义或实验预算不能作为“顺手修复”；超出当前授权时先报告。
* 遇到 artifact/checkpoint/schema 不匹配、NaN/Inf、writer 冲突或可能覆盖正式产物时，停止受影响的运行并报告证据。不绕过检查、静默丢弃异常或覆盖产物继续。
* 阻断后可继续只读定位；修复属于已授权范围时可修复并做局部验证。恢复执行前确认故障已解决，不扩大任务。
* 已退役实验不得自动重新进入生产路线。

预计超过一分钟、使用 GPU、启动 worker 或修改训练产物的命令，必须在 `tmux` session `Uni-Poly` 中运行并保留日志：

* 先检查现有 session 和任务，使用独立 window，避免重复启动或干扰已有进程。
* 记录实际命令、工作目录、window、日志路径和输出路径；按现有项目规范组织，不另建通用任务框架。
* `tmux` 只负责承载任务，不代表任务获得授权。不可用时先报告，不改用无日志后台运行绕过要求。

## 4\. Codex 与 ZCode 协作

默认分工为：**Codex 规划 → ZCode 执行 → Codex 审查 → 归档 → Codex 制定下一步计划**。使用根目录 [Plan.md](Plan.md) 交接，使用 [PROJECT_HISTORY.md](PROJECT_HISTORY.md) 保存每轮完整记录。

### 4.1 角色与职责

* **Codex 负责规划与审查**：核实项目状态，明确科学问题、实施方案、修改边界、验证预算和验收标准；审查实际 diff、日志与产物，区分已验证结论和推测。审查后必须提出并写明下一步计划，不能只列问题而不安排后续。
* **ZCode 负责具体执行**：按当前计划拆解步骤、实现代码、进行授权范围内的验证，并记录真实进度、失败、修复和交付物。普通实现细节自行决定；改变科学定义、验收标准或预算时，先记录问题，交回 Codex 规划，超出授权部分再由用户决定。
* ZCode 可以报告自检通过、执行完成或阻断，但不能代替 Codex 宣布最终验收通过。Codex 未检查的项目必须标为未核实；同一主体的自检不能写成独立审查。
* 默认 Codex 不接管 ZCode 的代码修改或训练执行；用户明确要求 Codex 实现、修复或修改文档时，该明确要求优先。记录实际执行者，不为形式上的分工重复工作。
* 不默认启动其他代理或并行执行者。不允许双方同时修改同一文件或写同一训练产物；接手前先核对最新文件、执行记录与活动进程。

### 4.2 `Plan.md`：唯一当前交接入口

每轮至少包含以下内容，不要求额外的 handoff 文件或任务框架：

|部分|维护者与必需内容|
|-|-|
|计划头|Codex：计划 ID、修订号、状态、授权来源、规划／执行／审查角色、基准 commit 与已有改动|
|实施计划|Codex：目标、实施步骤、允许与禁止的操作、接口变化、验证预算、停止条件及验收标准；科学比较还须满足第 5 节|
|执行记录|ZCode：实际修改、命令、执行环境、进度、结果、日志／产物路径、偏差及待审查问题|
|审查与下一步|Codex：逐项验收、证据、残留风险、通过／需返修／阻断结论，以及下一步目标、步骤、预算和授权边界|

* 状态使用“待授权、待执行、执行中、待审查、需返修、阻断、已完成、已取消”。状态必须说明依据；仅据用户报告的运行状态注明“用户报告，未独立核实”。
* ZCode 开始前读取最新 `AGENTS.md` 和 `Plan.md`，核对计划 ID／修订号与实际基线；在阶段结束、阻断和交付时更新执行记录，不把计划目标写成已完成事实。
* 执行中不静默替换目标或预算。必要修订由 Codex 更新修订号和变更理由，明确已执行步骤与剩余工作；ZCode 在下一阶段前确认修订，双方避免同时编辑交接文件。
* 审查优先核对本轮改动与风险；不因交接重新运行已充分验证的无关检查。需返修时保留同一计划 ID，增加修订并写出具体返修步骤。
* 审查通过后，Codex 先归档本轮，再将下一步完整计划写入 `Plan.md`。超出已有授权的下一步标为“待授权”，不能由执行者自动启动。任务已结束时明确“暂无后续执行”，不人为新增实验。
* 用户取消或替换计划时，先归档已有执行和审查状态，再放入新计划。阻断但尚未结束的计划保留在 `Plan.md`；不把未完成周期标为已完成。

### 4.3 `PROJECT_HISTORY.md`：每轮修改的完整存档

* 根目录只维护这一个周期归档文档，按关闭时间追加条目。归档单位是“一次计划的规划、执行、审查”，不是每个文件保存、命令或 commit。
* 由 Codex 在审查完成或周期被取消／替换时归档；ZCode 在 `Plan.md` 提供执行材料。仅修改文档的周期同样记录，注明自检与独立审查的区别。
* 每条记录包含：计划 ID／修订与日期、实际角色、授权与目标、最终实施计划和重要修订、实际文件与行为变化、验证命令和结果、日志／产物及可用 commit、审查结论、未完成项与原因、下一步及后续计划 ID。
* 将最终计划、执行摘要和审查结论实质性保存到归档，不能只链接即将被替换的 `Plan.md`；大段日志和 diff 链接到稳定产物或 commit，不重复粘贴。
* 先确认归档记录完整，再替换 `Plan.md`。同一周期不重复归档；需纠正历史记录时追加带日期的更正，不擦除原失败或改写当时结论。
* 不追溯编造旧周期。历史证据不足时标为“未核实”；进行中的工作不因建立归档机制而被宣布完成。
* `PIPELINE.md` 说明当前流程，`RESULTS.md` 记录实验结果，`PROJECT_HISTORY.md` 记录变更周期；只更新实际受影响的文档，不把归档写成新的授权来源。

## 5\. 科学实验与归因

每次科学比较先明确：

|项目|必须回答|
|-|-|
|scientific question|本次验证哪个假设？|
|reference|与哪个已核实的参考结果比较？|
|controlled change|本次实验只增加或改变什么？|
|比较预算|哪些数据、split、初始化、训练与评估条件保持一致？|

若仍有多项变化，明确混杂因素，不把差值自动解释为单一机制。若指标越低越好，说明差值方向，避免与 R² 的正增量混用。

其他约束：

* `fused - baseline` 不能自动称为 interaction；交互效应需要相应的组合消融设计。
* 改变 encoder 输入语义时，优先 matched pretraining / matched training；无法匹配时说明限制。
* 部分 task/fold/epoch 或小样本结果只能称为 smoke；即使包含消融组，也不能作为正式实验结论。
* 已参与模型开发的共享 validation/test fold 不是独立盲测。
* `results/best\_result.csv` 的宏平均是逐任务最优包络，不代表任何单模型。
* 候选完成正式复评前，不宣称已超过 baseline；可准确报告某次 smoke 的观测差值。
* 不以训练 loss、注意力权重或单次 seed 结果替代目标模型的正式预测评估。
* 已有充分失败证据的方向优先 STOP；只有新的明确假设及支持证据才考虑重启，不机械 sweep。
* 引用论文时区分原论文机制、项目改造和已验证结果，不把论文原型视为本项目性能保证。

## 6\. 验证预算与完成条件

除必要的局部单元测试外，模型、数据和训练验证默认只进行 smoke；不主动扩大 task、fold、epoch 或样本范围。正式实验另需明确授权。

只验证本次修改可能破坏的行为：

|修改类型|最小相关验证|
|-|-|
|文档或静态审查|检查相关引用、逻辑和格式，不启动模型任务|
|局部代码|相关单元测试|
|Dataset / collate / forward|小样本 forward/backward|
|periodic / geometry|本次涉及的 identity、shift、multiplicity、不变性或 fallback|
|training / GPU / scheduler|相关短 smoke|
|性能优化|先短 benchmark，再依据结果决定下一步|

* 优先使用现有、与风险对应的验证；不主动运行无关全仓测试或昂贵实验。
* 当前计划已有验证范围时遵守该范围；范围未指定时采用能覆盖改动风险的最小验证，不补成正式训练。
* 验证失败先定位原因，不靠增大样本、延长训练或扩大 sweep 掩盖实现问题。
* 必要风险已验证后停止追加测试；交付结果或继续下一项已授权工作。
* 因环境或数据缺失未执行的检查写明“未执行”，不能以推测代替结果。
* `py\_compile`、单元测试和短 smoke 均不能证明模型性能提升。

## 7\. 交付与状态记录

交付按实际任务简洁说明：

1. 做了什么，以及结论属于静态审查、计划、实现、测试、smoke、消融还是正式实验；
2. 修改了哪些文件；没有修改则注明；
3. 实际运行的测试或实验、范围及结果；
4. 尚未执行的内容、现实阻断与下一步建议；
5. 如有长任务，提供 `tmux` window、日志和产物路径，并区分已启动、运行中、失败和已完成。

不把“代码已写好”称为“测试通过”，不把“任务已启动”称为“实验完成”，不把“smoke 通过”称为“性能提升”。执行交付先写入 `Plan.md`，由 Codex 审查并按第 4 节归档；仅在本次变更影响现有流程或结果记录时更新 `PIPELINE.md` 或 `RESULTS.md`，不另建重复报告或 handoff。
