Task：正式实现 Uni-Poly-Plus 的第一版 Knowledge
Graph Module
你已经完成了项目分析和 KG 设计。
现在开始进入实现阶段。
请严格按照之前确定的技术路线实现，不要重新设计架构。
当前状态
已经准备好的资源：
knowledge_graph/KANO_initial/
包含：
ele2emb.pkl
fg2emb.pkl
rel2emb.pkl
elementkgontology.embeddings.txt
objectproperty.txt
knowledge_graph/mappings/
包含：
functional_group_smarts.json
entity_name_alias.json
element_symbol_mapping.json
当前 Uni-Poly-Plus 已经支持：
smiles
graph
fp
geom
四种模态。
实现目标
实现第一版 KG 模态：
• 
• 
• 
• 
• 
• 
• 
• 
• 
• 
• 
• 
1

text
kg
并作为第五个模态接入当前框架：
text
smiles + graph + fp + geom + kg
→ FusionModule
→ prediction
重要约束（必须遵守）
不允许
不要：
重构项目
修改 FusionModule 主体逻辑
修改 GIN
修改 SchNet
修改 PaiNN
修改 graph_data.py
修改 geom_data.py
引入 Neo4j
引入在线知识图谱查询
引入新的大型依赖
实现 Graph Prompt
实现 KG-Augmented GNN
实现 R-GCN
实现 GAT
第一版只做：
text
离线 KANO embedding
+ KG modality
KG 模块设计
严格采用：
• 
• 
• 
• 
• 
• 
• 
• 
• 
• 
• 
• 
• 
• 
2

text
kg_entity_ids
+ kg_mask ↓ Embedding Lookup ↓ Attention Pooling ↓ Projection ↓ [B,256]
输出
然后作为新模态加入：
text
modal_embeddings
实现步骤
请按以下顺序完成。
Step 1
检查并修正：
knowledge_graph/mappings/element_symbol_mapping.json
要求：
从：
text
"C" → "Carbon"
改为：
text
"C" → 5 "N" → 6 "O" → 7 ...
即：
元素符号
→
KANO ele2emb.pkl 对应 index
3

如果文件已经正确，则无需修改。
Step 2
新增 KG 工具模块
目录：
text
src/kg/
建议：
text
src/kg/ ├── kg_utils.py ├── kg_loader.py ├── kg_mapping.py
职责：
kg_loader.py
负责：
加载 ele2emb.pkl
加载 fg2emb.pkl
kg_mapping.py
负责：
SMILES
↓
元素提取
↓
官能团检测
↓
KG entity ids
kg_utils.py
负责：
• 
• 
4

padding mask helper functions
Step 3
实现样本级 KG 映射
输入：
text
SMILES
输出：
text
kg_entity_ids kg_mask
映射规则：
元素：
text
RDKit atom.GetAtomicNum()
↓
atomic_number - 1
↓
ele2emb key
官能团：
使用：
functional_group_smarts.json
进行 SMARTS 匹配
↓
entity_name_alias.json
统一名称
5

↓
fg2emb key
最终：
一个样本得到：
text
[element_entities + functional_group_entities]
Step 4
实现 KGEncoder
新增：
text
src/modules/kg.py
实现：
text
class KGEncoder(nn.Module)
输入：
kg_entity_ids kg_mask
流程：
Embedding Lookup
↓
Mask-Aware Attention Pooling
↓
Linear Projection
↓
256维输出
6

输出：
text
[B,256]
要求：
保持接口风格与其他 encoder 一致。
Step 5
修改 Dataset
修改：
text
src/dataset/dataset.py
要求：
每个样本额外返回：
text
kg_entity_ids kg_mask
不要破坏原有字段。
Step 6
修改 Dataloader
修改：
text
src/dataset/dataloader.py
要求：
支持 batch：
text
7

kg_entity_ids kg_mask
padding
mask
Step 7
接入 UniEncoderAttention
修改：
text
src/modules/uni_encoder.py
要求：
新增：
text
self.kg_encoder
支持：
text
modality="kg"
KG embedding：
text
[B,256]
加入：
text
modal_embeddings
不要改 FusionModule 逻辑。
8

Step 8
修改训练脚本
修改：
text
scripts/train.py scripts/pretrain.py
要求：
当：
text
--modalities kg
时自动启用 KG。
例如：
text
--modalities smiles graph fp geom kg
能够正常运行。
Step 9
扩展 Contrastive Learning
预训练阶段：
当前：
text
smiles graph fp geom
扩展为：
text
smiles graph fp geom kg
使用现有：
9

compute_contrastive_loss()
不要新增新的 loss。
Step 10
增加配置项
要求：
可以通过配置关闭 KG：
例如：
text
use_kg=True
或：
text
modalities=['smiles' , 'graph' , 'fp' , 'geom']
此时完全不加载 KG。
验收标准
完成后请输出：
1
修改了哪些文件
2
新增了哪些文件
3
每个文件修改内容
4
完整数据流
10

从：
SMILES
↓
KG entity extraction
↓
KG embedding
↓
Fusion
↓
Prediction
5
所有 tensor shape
例如：
text
kg_entity_ids: [B,K]
kg_mask: [B,K]
kg_embedding: [B,256]
modal_embeddings: [B,5,256]
6
可能存在的 Bug 或风险点
7
给出建议运行命令
例如：
python scripts/pretrain.py ... python scripts/train.py ...
11

在完成上述分析前，不要提交代码 Patch。
先完成实现方案审查，然后再开始真正修改代码。
12